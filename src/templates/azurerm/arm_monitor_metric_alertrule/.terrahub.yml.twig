  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_monitor_metric_alertrule:
        {{ name }}:
          name: var.{{ name }}_name
          threshold: var.{{ name }}_threshold
          resource_id: var.{{ name }}_resource_id
          metric_name: var.{{ name }}_metric_name
          operator: var.{{ name }}_operator
          period: var.{{ name }}_period
          aggregation: var.{{ name }}_aggregation
          # webhook_action: 
          #   -
          #     service_uri: ''
          #     properties: 
          #       '[TO_BE_REPLACED]': ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
          # description: ''
          # enabled: false
          # email_action: 
          #   -
          #     send_to_service_owners: false
          #     custom_emails: 
          #       -
    variable:
      {{ name }}_name:
        type: string
      {{ name }}_threshold:
        type: float
      {{ name }}_resource_id:
        type: string
      {{ name }}_metric_name:
        type: string
      {{ name }}_operator:
        type: string
      {{ name }}_period:
        type: string
      {{ name }}_aggregation:
        type: string
    output:
      id:
        value: azurerm_arm_monitor_metric_alertrule.{{ name }}.id
      thub_id:
        value: azurerm_arm_monitor_metric_alertrule.{{ name }}.id
      webhook_action:
        value: azurerm_arm_monitor_metric_alertrule.{{ name }}.webhook_action
      tags:
        value: azurerm_arm_monitor_metric_alertrule.{{ name }}.tags
      description:
        value: azurerm_arm_monitor_metric_alertrule.{{ name }}.description
      email_action:
        value: azurerm_arm_monitor_metric_alertrule.{{ name }}.email_action
