  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_logic_app_trigger_recurrence:
        {{ name }}:
          frequency: var.{{ name }}_frequency
          interval: var.{{ name }}_interval
          name: var.{{ name }}_name
          logic_app_id: var.{{ name }}_logic_app_id
    variable:
      {{ name }}_frequency:
        type: string
      {{ name }}_interval:
        type: int
      {{ name }}_name:
        type: string
      {{ name }}_logic_app_id:
        type: string
    output:
      id:
        value: azurerm_arm_logic_app_trigger_recurrence.{{ name }}.id
      thub_id:
        value: azurerm_arm_logic_app_trigger_recurrence.{{ name }}.id
