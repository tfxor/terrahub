  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_firewall_network_rule_collection:
        {{ name }}:
          rule:
            source_addresses: []
            destination_addresses: []
            destination_ports: []
            protocols: []
            name: ''
          #   description: ''
          name: '${var.{{ name }}_name}'
          azure_firewall_name: '${var.{{ name }}_azure_firewall_name}'
          priority: '${var.{{ name }}_priority}'
          action: '${var.{{ name }}_action}'
    variable:
      {{ name }}_name:
        type: 'string'
      {{ name }}_azure_firewall_name:
        type: 'string'
      {{ name }}_priority:
        type: 'int'
      {{ name }}_action:
        type: 'string'
    output:
      id:
        value: '${azurerm_arm_firewall_network_rule_collection.{{ name }}.id}'
      thub_id:
        value: '${azurerm_arm_firewall_network_rule_collection.{{ name }}.id}'
