  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_policy_definition:
        {{ name }}:
          display_name: '${var.{{ name }}_display_name}'
          mode: '${var.{{ name }}_mode}'
          name: '${var.{{ name }}_name}'
          policy_type: '${var.{{ name }}_policy_type}'
          # policy_rule: ''
          # parameters: ''
          # description: ''
          # management_group_id: ''
          # metadata: ''
    variable:
      {{ name }}_display_name:
        type: 'string'
      {{ name }}_mode:
        type: 'string'
      {{ name }}_name:
        type: 'string'
      {{ name }}_policy_type:
        type: 'string'
    output:
      id:
        value: '${azurerm_arm_policy_definition.{{ name }}.id}'
      thub_id:
        value: '${azurerm_arm_policy_definition.{{ name }}.id}'
