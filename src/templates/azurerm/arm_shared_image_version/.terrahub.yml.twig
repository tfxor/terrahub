  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_shared_image_version:
        {{ name }}:
          gallery_name: var.{{ name }}_gallery_name
          image_name: var.{{ name }}_image_name
          managed_image_id: var.{{ name }}_managed_image_id
          target_region: 
            name: ''
            regional_replica_count: 0
          name: var.{{ name }}_name
          # exclude_from_latest: false
          # tags: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_gallery_name:
        type: string
      {{ name }}_image_name:
        type: string
      {{ name }}_managed_image_id:
        type: string
      {{ name }}_name:
        type: string
    output:
      id:
        value: azurerm_arm_shared_image_version.{{ name }}.id
      thub_id:
        value: azurerm_arm_shared_image_version.{{ name }}.id
