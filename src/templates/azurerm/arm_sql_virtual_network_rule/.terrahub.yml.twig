  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_sql_virtual_network_rule:
        {{ name }}:
          subnet_id: '${var.{{ name }}_subnet_id}'
          name: '${var.{{ name }}_name}'
          server_name: '${var.{{ name }}_server_name}'
          # ignore_missing_vnet_service_endpoint: false
    variable:
      {{ name }}_subnet_id:
        type: 'string'
      {{ name }}_name:
        type: 'string'
      {{ name }}_server_name:
        type: 'string'
    output:
      id:
        value: '${azurerm_arm_sql_virtual_network_rule.{{ name }}.id}'
      thub_id:
        value: '${azurerm_arm_sql_virtual_network_rule.{{ name }}.id}'
