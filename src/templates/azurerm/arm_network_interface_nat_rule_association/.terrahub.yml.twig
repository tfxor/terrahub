  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_network_interface_nat_rule_association:
        {{ name }}:
          nat_rule_id: '${var.{{ name }}_nat_rule_id}'
          network_interface_id: '${var.{{ name }}_network_interface_id}'
          ip_configuration_name: '${var.{{ name }}_ip_configuration_name}'
    variable:
      {{ name }}_nat_rule_id:
        type: 'string'
      {{ name }}_network_interface_id:
        type: 'string'
      {{ name }}_ip_configuration_name:
        type: 'string'
    output:
      id:
        value: '${azurerm_arm_network_interface_nat_rule_association.{{ name }}.id}'
      thub_id:
        value: '${azurerm_arm_network_interface_nat_rule_association.{{ name }}.id}'
