  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_traffic_manager_profile:
        {{ name }}:
          monitor_config: 
            port: 0
            protocol: ''
          #   path: ''
          name: var.{{ name }}_name
          dns_config: 
            relative_name: ''
            ttl: 0
          traffic_routing_method: var.{{ name }}_traffic_routing_method
          # profile_status: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_name:
        type: string
      {{ name }}_traffic_routing_method:
        type: string
    output:
      id:
        value: azurerm_arm_traffic_manager_profile.{{ name }}.id
      thub_id:
        value: azurerm_arm_traffic_manager_profile.{{ name }}.id
      fqdn:
        value: azurerm_arm_traffic_manager_profile.{{ name }}.fqdn
      profile_status:
        value: azurerm_arm_traffic_manager_profile.{{ name }}.profile_status
