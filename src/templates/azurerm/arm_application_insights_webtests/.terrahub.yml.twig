  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_application_insights_webtests:
        {{ name }}:
          geo_locations: var.{{ name }}_geo_locations
          name: var.{{ name }}_name
          configuration: var.{{ name }}_configuration
          application_insights_id: var.{{ name }}_application_insights_id
          kind: var.{{ name }}_kind
          # enabled: false
          # description: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
          # timeout: 0
          # retry_enabled: false
          # frequency: 0
    variable:
      {{ name }}_geo_locations:
        type: list
      {{ name }}_name:
        type: string
      {{ name }}_configuration:
        type: string
      {{ name }}_application_insights_id:
        type: string
      {{ name }}_kind:
        type: string
    output:
      id:
        value: azurerm_arm_application_insights_webtests.{{ name }}.id
      thub_id:
        value: azurerm_arm_application_insights_webtests.{{ name }}.id
      synthetic_monitor_id:
        value: azurerm_arm_application_insights_webtests.{{ name }}.synthetic_monitor_id
