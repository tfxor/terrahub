  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_azuread_service_principal_password:
        {{ name }}:
          service_principal_id: '${var.{{ name }}_service_principal_id}'
          value: '${var.{{ name }}_value}'
          end_date: '${var.{{ name }}_end_date}'
          # key_id: ''
          # start_date: ''
    variable:
      {{ name }}_service_principal_id:
        type: 'string'
      {{ name }}_value:
        type: 'string'
      {{ name }}_end_date:
        type: 'string'
    output:
      id:
        value: '${azurerm_arm_azuread_service_principal_password.{{ name }}.id}'
      thub_id:
        value: '${azurerm_arm_azuread_service_principal_password.{{ name }}.id}'
      key_id:
        value: '${azurerm_arm_azuread_service_principal_password.{{ name }}.key_id}'
      start_date:
        value: '${azurerm_arm_azuread_service_principal_password.{{ name }}.start_date}'
