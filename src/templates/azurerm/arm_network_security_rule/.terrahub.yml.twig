  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_network_security_rule:
        {{ name }}:
          network_security_group_name: var.{{ name }}_network_security_group_name
          access: var.{{ name }}_access
          priority: var.{{ name }}_priority
          name: var.{{ name }}_name
          protocol: var.{{ name }}_protocol
          direction: var.{{ name }}_direction
          # destination_address_prefixes: []
          # source_application_security_group_ids: []
          # destination_port_range: ''
          # source_address_prefixes: []
          # destination_address_prefix: ''
          # destination_application_security_group_ids: []
          # description: ''
          # source_port_range: ''
          # source_port_ranges: []
          # destination_port_ranges: []
          # source_address_prefix: ''
    variable:
      {{ name }}_network_security_group_name:
        type: string
      {{ name }}_access:
        type: string
      {{ name }}_priority:
        type: int
      {{ name }}_name:
        type: string
      {{ name }}_protocol:
        type: string
      {{ name }}_direction:
        type: string
    output:
      id:
        value: azurerm_arm_network_security_rule.{{ name }}.id
      thub_id:
        value: azurerm_arm_network_security_rule.{{ name }}.id
