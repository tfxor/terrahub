  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_storage_queue:
        {{ name }}:
          storage_account_name: '${var.{{ name }}_storage_account_name}'
          name: '${var.{{ name }}_name}'
    variable:
      {{ name }}_storage_account_name:
        type: 'string'
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${azurerm_arm_storage_queue.{{ name }}.id}'
      thub_id:
        value: '${azurerm_arm_storage_queue.{{ name }}.id}'
