  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_devspace_controller:
        {{ name }}:
          sku:
            -
              name: ''
              tier: ''
          target_container_host_resource_id: '${var.{{ name }}_target_container_host_resource_id}'
          target_container_host_credentials_base64: '${var.{{ name }}_target_container_host_credentials_base64}'
          name: '${var.{{ name }}_name}'
          host_suffix: '${var.{{ name }}_host_suffix}'
          # tags:
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_target_container_host_resource_id:
        type: 'string'
      {{ name }}_target_container_host_credentials_base64:
        type: 'string'
      {{ name }}_name:
        type: 'string'
      {{ name }}_host_suffix:
        type: 'string'
    output:
      id:
        value: '${azurerm_arm_devspace_controller.{{ name }}.id}'
      thub_id:
        value: '${azurerm_arm_devspace_controller.{{ name }}.id}'
      data_plane_fqdn:
        value: '${azurerm_arm_devspace_controller.{{ name }}.data_plane_fqdn}'
