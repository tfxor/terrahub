  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_loadbalancer_backend_address_pool:
        {{ name }}:
          loadbalancer_id: var.{{ name }}_loadbalancer_id
          name: var.{{ name }}_name
    variable:
      {{ name }}_loadbalancer_id:
        type: string
      {{ name }}_name:
        type: string
    output:
      id:
        value: azurerm_arm_loadbalancer_backend_address_pool.{{ name }}.id
      thub_id:
        value: azurerm_arm_loadbalancer_backend_address_pool.{{ name }}.id
      backend_ip_configurations:
        value: azurerm_arm_loadbalancer_backend_address_pool.{{ name }}.backend_ip_configurations
      load_balancing_rules:
        value: azurerm_arm_loadbalancer_backend_address_pool.{{ name }}.load_balancing_rules
