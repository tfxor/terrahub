  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_mariadb_server:
        {{ name }}:
          administrator_login: var.{{ name }}_administrator_login
          name: var.{{ name }}_name
          sku: 
            -
              name: ''
              capacity: 0
              tier: ''
              family: ''
          administrator_login_password: var.{{ name }}_administrator_login_password
          version: var.{{ name }}_version
          storage_profile: 
            -
              storage_mb: 0
          #     geo_redundant_backup: ''
          #     backup_retention_days: 0
          ssl_enforcement: var.{{ name }}_ssl_enforcement
          # tags: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_administrator_login:
        type: string
      {{ name }}_name:
        type: string
      {{ name }}_administrator_login_password:
        type: string
      {{ name }}_version:
        type: string
      {{ name }}_ssl_enforcement:
        type: string
    output:
      id:
        value: azurerm_arm_mariadb_server.{{ name }}.id
      thub_id:
        value: azurerm_arm_mariadb_server.{{ name }}.id
      fqdn:
        value: azurerm_arm_mariadb_server.{{ name }}.fqdn
