  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_postgresql_configuration:
        {{ name }}:
          server_name: '${var.{{ name }}_server_name}'
          value: '${var.{{ name }}_value}'
          name: '${var.{{ name }}_name}'
    variable:
      {{ name }}_server_name:
        type: 'string'
      {{ name }}_value:
        type: 'string'
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${azurerm_arm_postgresql_configuration.{{ name }}.id}'
      thub_id:
        value: '${azurerm_arm_postgresql_configuration.{{ name }}.id}'
