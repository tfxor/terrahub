  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_snapshot:
        {{ name }}:
          create_option: '${var.{{ name }}_create_option}'
          name: '${var.{{ name }}_name}'
          # source_uri: ''
          # source_resource_id: ''
          # storage_account_id: ''
          # disk_size_gb: 0
          # tags:
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_create_option:
        type: 'string'
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${azurerm_arm_snapshot.{{ name }}.id}'
      thub_id:
        value: '${azurerm_arm_snapshot.{{ name }}.id}'
      disk_size_gb:
        value: '${azurerm_arm_snapshot.{{ name }}.disk_size_gb}'
