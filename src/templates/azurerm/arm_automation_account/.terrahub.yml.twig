  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_automation_account:
        {{ name }}:
          sku:
            -
          #     name: ''
          name: '${var.{{ name }}_name}'
          # tags:
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${azurerm_arm_automation_account.{{ name }}.id}'
      thub_id:
        value: '${azurerm_arm_automation_account.{{ name }}.id}'
      dsc_server_endpoint:
        value: '${azurerm_arm_automation_account.{{ name }}.dsc_server_endpoint}'
      dsc_primary_access_key:
        value: '${azurerm_arm_automation_account.{{ name }}.dsc_primary_access_key}'
      dsc_secondary_access_key:
        value: '${azurerm_arm_automation_account.{{ name }}.dsc_secondary_access_key}'
