  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_automation_runbook:
        {{ name }}:
          publish_content_link: 
            -
              uri: ''
          #     version: ''
          #     hash: 
          #       -
          #         algorithm: ''
          #         value: ''
          name: var.{{ name }}_name
          runbook_type: var.{{ name }}_runbook_type
          log_verbose: var.{{ name }}_log_verbose
          account_name: var.{{ name }}_account_name
          log_progress: var.{{ name }}_log_progress
          # description: ''
          # content: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_name:
        type: string
      {{ name }}_runbook_type:
        type: string
      {{ name }}_log_verbose:
        type: bool
      {{ name }}_account_name:
        type: string
      {{ name }}_log_progress:
        type: bool
    output:
      id:
        value: azurerm_arm_automation_runbook.{{ name }}.id
      thub_id:
        value: azurerm_arm_automation_runbook.{{ name }}.id
      content:
        value: azurerm_arm_automation_runbook.{{ name }}.content
