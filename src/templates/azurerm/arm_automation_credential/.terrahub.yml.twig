  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_automation_credential:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          account_name: '${var.{{ name }}_account_name}'
          username: '${var.{{ name }}_username}'
          password: '${var.{{ name }}_password}'
          # description: ''
    variable:
      {{ name }}_name:
        type: 'string'
      {{ name }}_account_name:
        type: 'string'
      {{ name }}_username:
        type: 'string'
      {{ name }}_password:
        type: 'string'
    output:
      id:
        value: '${azurerm_arm_automation_credential.{{ name }}.id}'
      thub_id:
        value: '${azurerm_arm_automation_credential.{{ name }}.id}'
