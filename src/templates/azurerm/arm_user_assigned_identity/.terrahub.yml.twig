  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_user_assigned_identity:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          # tags:
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${azurerm_arm_user_assigned_identity.{{ name }}.id}'
      thub_id:
        value: '${azurerm_arm_user_assigned_identity.{{ name }}.id}'
      principal_id:
        value: '${azurerm_arm_user_assigned_identity.{{ name }}.principal_id}'
      client_id:
        value: '${azurerm_arm_user_assigned_identity.{{ name }}.client_id}'
