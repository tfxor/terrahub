  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_scheduler_job_collection:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          sku: '${var.{{ name }}_sku}'
          # state: ''
          # quota:
          #   -
          #     max_recurrence_frequency: ''
          #     max_job_count: 0
          #     max_retry_interval: 0
          #     max_recurrence_interval: 0
          # tags:
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_name:
        type: 'string'
      {{ name }}_sku:
        type: 'string'
    output:
      id:
        value: '${azurerm_arm_scheduler_job_collection.{{ name }}.id}'
      thub_id:
        value: '${azurerm_arm_scheduler_job_collection.{{ name }}.id}'
