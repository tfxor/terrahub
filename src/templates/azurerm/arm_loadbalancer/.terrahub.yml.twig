  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_loadbalancer:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          # sku: ''
          # frontend_ip_configuration: 
          #   -
          #     name: ''
          #     subnet_id: ''
          #     private_ip_address: ''
          #     public_ip_address_id: ''
          #     private_ip_address_allocation: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${azurerm_arm_loadbalancer.{{ name }}.id}'
      thub_id:
        value: '${azurerm_arm_loadbalancer.{{ name }}.id}'
      private_ip_address:
        value: '${azurerm_arm_loadbalancer.{{ name }}.private_ip_address}'
      private_ip_addresses:
        value: '${azurerm_arm_loadbalancer.{{ name }}.private_ip_addresses}'
