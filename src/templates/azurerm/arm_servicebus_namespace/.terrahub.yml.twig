  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_servicebus_namespace:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          sku: '${var.{{ name }}_sku}'
          # capacity: 0
          # tags:
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_name:
        type: 'string'
      {{ name }}_sku:
        type: 'string'
    output:
      id:
        value: '${azurerm_arm_servicebus_namespace.{{ name }}.id}'
      thub_id:
        value: '${azurerm_arm_servicebus_namespace.{{ name }}.id}'
      default_primary_connection_string:
        value: '${azurerm_arm_servicebus_namespace.{{ name }}.default_primary_connection_string}'
      default_secondary_connection_string:
        value: '${azurerm_arm_servicebus_namespace.{{ name }}.default_secondary_connection_string}'
      default_primary_key:
        value: '${azurerm_arm_servicebus_namespace.{{ name }}.default_primary_key}'
      default_secondary_key:
        value: '${azurerm_arm_servicebus_namespace.{{ name }}.default_secondary_key}'
