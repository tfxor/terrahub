  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_logic_app_action_http:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          logic_app_id: '${var.{{ name }}_logic_app_id}'
          method: '${var.{{ name }}_method}'
          uri: '${var.{{ name }}_uri}'
          # headers:
          #   '[TO_BE_REPLACED]': ''
          # body: ''
    variable:
      {{ name }}_name:
        type: 'string'
      {{ name }}_logic_app_id:
        type: 'string'
      {{ name }}_method:
        type: 'string'
      {{ name }}_uri:
        type: 'string'
    output:
      id:
        value: '${azurerm_arm_logic_app_action_http.{{ name }}.id}'
      thub_id:
        value: '${azurerm_arm_logic_app_action_http.{{ name }}.id}'
