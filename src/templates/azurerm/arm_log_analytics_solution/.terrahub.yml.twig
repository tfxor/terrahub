  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_log_analytics_solution:
        {{ name }}:
          solution_name: '${var.{{ name }}_solution_name}'
          workspace_name: '${var.{{ name }}_workspace_name}'
          workspace_resource_id: '${var.{{ name }}_workspace_resource_id}'
          plan:
            -
              publisher: ''
              product: ''
          #     promotion_code: ''
    variable:
      {{ name }}_solution_name:
        type: 'string'
      {{ name }}_workspace_name:
        type: 'string'
      {{ name }}_workspace_resource_id:
        type: 'string'
    output:
      id:
        value: '${azurerm_arm_log_analytics_solution.{{ name }}.id}'
      thub_id:
        value: '${azurerm_arm_log_analytics_solution.{{ name }}.id}'
