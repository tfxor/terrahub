  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_servicebus_topic:
        {{ name }}:
          namespace_name: '${var.{{ name }}_namespace_name}'
          name: '${var.{{ name }}_name}'
          # status: ''
          # duplicate_detection_history_time_window: ''
          # enable_partitioning: false
          # enable_filtering_messages_before_publishing: false
          # auto_delete_on_idle: ''
          # max_size_in_megabytes: 0
          # default_message_ttl: ''
          # enable_batched_operations: false
          # support_ordering: false
          # enable_express: false
          # requires_duplicate_detection: false
    variable:
      {{ name }}_namespace_name:
        type: 'string'
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${azurerm_arm_servicebus_topic.{{ name }}.id}'
      thub_id:
        value: '${azurerm_arm_servicebus_topic.{{ name }}.id}'
      duplicate_detection_history_time_window:
        value: '${azurerm_arm_servicebus_topic.{{ name }}.duplicate_detection_history_time_window}'
      auto_delete_on_idle:
        value: '${azurerm_arm_servicebus_topic.{{ name }}.auto_delete_on_idle}'
      max_size_in_megabytes:
        value: '${azurerm_arm_servicebus_topic.{{ name }}.max_size_in_megabytes}'
      default_message_ttl:
        value: '${azurerm_arm_servicebus_topic.{{ name }}.default_message_ttl}'
