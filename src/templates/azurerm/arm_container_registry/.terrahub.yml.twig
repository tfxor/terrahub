  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_container_registry:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          # storage_account_id: ''
          # storage_account: []
          # sku: ''
          # admin_enabled: false
          # georeplication_locations: []
          # tags: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${azurerm_arm_container_registry.{{ name }}.id}'
      thub_id:
        value: '${azurerm_arm_container_registry.{{ name }}.id}'
      login_server:
        value: '${azurerm_arm_container_registry.{{ name }}.login_server}'
      admin_username:
        value: '${azurerm_arm_container_registry.{{ name }}.admin_username}'
      admin_password:
        value: '${azurerm_arm_container_registry.{{ name }}.admin_password}'
