  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_storage_table_entity:
        {{ name }}:
          entity: var.{{ name }}_entity
          table_name: var.{{ name }}_table_name
          storage_account_name: var.{{ name }}_storage_account_name
          partition_key: var.{{ name }}_partition_key
          row_key: var.{{ name }}_row_key
    variable:
      {{ name }}_entity:
        type: map
      {{ name }}_table_name:
        type: string
      {{ name }}_storage_account_name:
        type: string
      {{ name }}_partition_key:
        type: string
      {{ name }}_row_key:
        type: string
    output:
      id:
        value: azurerm_arm_storage_table_entity.{{ name }}.id
      thub_id:
        value: azurerm_arm_storage_table_entity.{{ name }}.id
