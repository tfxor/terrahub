  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_sql_firewall_rule:
        {{ name }}:
          name: var.{{ name }}_name
          server_name: var.{{ name }}_server_name
          start_ip_address: var.{{ name }}_start_ip_address
          end_ip_address: var.{{ name }}_end_ip_address
    variable:
      {{ name }}_name:
        type: string
      {{ name }}_server_name:
        type: string
      {{ name }}_start_ip_address:
        type: string
      {{ name }}_end_ip_address:
        type: string
    output:
      id:
        value: azurerm_arm_sql_firewall_rule.{{ name }}.id
      thub_id:
        value: azurerm_arm_sql_firewall_rule.{{ name }}.id
