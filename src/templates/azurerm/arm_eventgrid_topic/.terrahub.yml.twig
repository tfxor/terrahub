  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_eventgrid_topic:
        {{ name }}:
          name: var.{{ name }}_name
          # tags: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_name:
        type: string
    output:
      id:
        value: azurerm_arm_eventgrid_topic.{{ name }}.id
      thub_id:
        value: azurerm_arm_eventgrid_topic.{{ name }}.id
      endpoint:
        value: azurerm_arm_eventgrid_topic.{{ name }}.endpoint
      primary_access_key:
        value: azurerm_arm_eventgrid_topic.{{ name }}.primary_access_key
      secondary_access_key:
        value: azurerm_arm_eventgrid_topic.{{ name }}.secondary_access_key
