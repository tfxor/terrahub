  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_cosmosdb_mongo_collection:
        {{ name }}:
          name: var.{{ name }}_name
          account_name: var.{{ name }}_account_name
          database_name: var.{{ name }}_database_name
          # shard_key: ''
          # default_ttl_seconds: 0
          # indexes: 
          #   key: ''
          #   unique: false
    variable:
      {{ name }}_name:
        type: string
      {{ name }}_account_name:
        type: string
      {{ name }}_database_name:
        type: string
    output:
      id:
        value: azurerm_arm_cosmosdb_mongo_collection.{{ name }}.id
      thub_id:
        value: azurerm_arm_cosmosdb_mongo_collection.{{ name }}.id
