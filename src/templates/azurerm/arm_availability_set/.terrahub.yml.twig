  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_availability_set:
        {{ name }}:
          name: var.{{ name }}_name
          # managed: false
          # tags: 
          #   '[TO_BE_REPLACED]': ''
          # platform_update_domain_count: 0
          # platform_fault_domain_count: 0
    variable:
      {{ name }}_name:
        type: string
    output:
      id:
        value: azurerm_arm_availability_set.{{ name }}.id
      thub_id:
        value: azurerm_arm_availability_set.{{ name }}.id
