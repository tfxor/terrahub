  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_data_lake_store_file:
        {{ name }}:
          remote_file_path: var.{{ name }}_remote_file_path
          local_file_path: var.{{ name }}_local_file_path
          account_name: var.{{ name }}_account_name
    variable:
      {{ name }}_remote_file_path:
        type: string
      {{ name }}_local_file_path:
        type: string
      {{ name }}_account_name:
        type: string
    output:
      id:
        value: azurerm_arm_data_lake_store_file.{{ name }}.id
      thub_id:
        value: azurerm_arm_data_lake_store_file.{{ name }}.id
