  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_public_ip:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          # public_ip_address_allocation: ''
          # idle_timeout_in_minutes: 0
          # allocation_method: ''
          # sku: ''
          # ip_version: ''
          # domain_name_label: ''
          # reverse_fqdn: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${azurerm_arm_public_ip.{{ name }}.id}'
      thub_id:
        value: '${azurerm_arm_public_ip.{{ name }}.id}'
      allocation_method:
        value: '${azurerm_arm_public_ip.{{ name }}.allocation_method}'
      ip_address:
        value: '${azurerm_arm_public_ip.{{ name }}.ip_address}'
      fqdn:
        value: '${azurerm_arm_public_ip.{{ name }}.fqdn}'
