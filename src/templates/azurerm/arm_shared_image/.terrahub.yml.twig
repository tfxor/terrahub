  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_shared_image:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          gallery_name: '${var.{{ name }}_gallery_name}'
          os_type: '${var.{{ name }}_os_type}'
          identifier: 
            -
              publisher: ''
              offer: ''
              sku: ''
          # description: ''
          # eula: ''
          # privacy_statement_uri: ''
          # release_note_uri: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_name:
        type: 'string'
      {{ name }}_gallery_name:
        type: 'string'
      {{ name }}_os_type:
        type: 'string'
    output:
      id:
        value: '${azurerm_arm_shared_image.{{ name }}.id}'
      thub_id:
        value: '${azurerm_arm_shared_image.{{ name }}.id}'
