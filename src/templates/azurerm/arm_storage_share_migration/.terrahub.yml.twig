  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_storage_share_migration:
        {{ name }}:
          name: var.{{ name }}_name
          storage_account_name: var.{{ name }}_storage_account_name
          # quota: 0
    variable:
      {{ name }}_name:
        type: string
      {{ name }}_storage_account_name:
        type: string
    output:
      url:
        value: azurerm_arm_storage_share_migration.{{ name }}.url
