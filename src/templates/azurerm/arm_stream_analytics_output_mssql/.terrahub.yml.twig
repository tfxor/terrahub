  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_stream_analytics_output_mssql:
        {{ name }}:
          name: var.{{ name }}_name
          stream_analytics_job_name: var.{{ name }}_stream_analytics_job_name
          resource_group_name: var.{{ name }}_resource_group_name
          server: var.{{ name }}_server
          database: var.{{ name }}_database
          table: var.{{ name }}_table
          user: var.{{ name }}_user
          password: var.{{ name }}_password
    variable:
      {{ name }}_name:
        type: string
      {{ name }}_stream_analytics_job_name:
        type: string
      {{ name }}_resource_group_name:
        type: string
      {{ name }}_server:
        type: string
      {{ name }}_database:
        type: string
      {{ name }}_table:
        type: string
      {{ name }}_user:
        type: string
      {{ name }}_password:
        type: string
    output:
      id:
        value: azurerm_arm_stream_analytics_output_mssql.{{ name }}.id
      thub_id:
        value: azurerm_arm_stream_analytics_output_mssql.{{ name }}.id
