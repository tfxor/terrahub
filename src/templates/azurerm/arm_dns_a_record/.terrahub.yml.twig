  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_dns_a_record:
        {{ name }}:
          ttl: '${var.{{ name }}_ttl}'
          name: '${var.{{ name }}_name}'
          zone_name: '${var.{{ name }}_zone_name}'
          records: '${var.{{ name }}_records}'
          # tags: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_ttl:
        type: 'int'
      {{ name }}_name:
        type: 'string'
      {{ name }}_zone_name:
        type: 'string'
      {{ name }}_records:
        type: 'set'
    output:
      id:
        value: '${azurerm_arm_dns_a_record.{{ name }}.id}'
      thub_id:
        value: '${azurerm_arm_dns_a_record.{{ name }}.id}'
