  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_cdn_profile:
        {{ name }}:
          name: var.{{ name }}_name
          sku: var.{{ name }}_sku
          # tags: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_name:
        type: string
      {{ name }}_sku:
        type: string
    output:
      id:
        value: azurerm_arm_cdn_profile.{{ name }}.id
      thub_id:
        value: azurerm_arm_cdn_profile.{{ name }}.id
