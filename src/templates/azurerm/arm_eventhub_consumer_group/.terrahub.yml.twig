  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_eventhub_consumer_group:
        {{ name }}:
          name: var.{{ name }}_name
          namespace_name: var.{{ name }}_namespace_name
          eventhub_name: var.{{ name }}_eventhub_name
          # user_metadata: ''
    variable:
      {{ name }}_name:
        type: string
      {{ name }}_namespace_name:
        type: string
      {{ name }}_eventhub_name:
        type: string
    output:
      id:
        value: azurerm_arm_eventhub_consumer_group.{{ name }}.id
      thub_id:
        value: azurerm_arm_eventhub_consumer_group.{{ name }}.id
