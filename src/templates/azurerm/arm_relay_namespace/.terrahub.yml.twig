  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_relay_namespace:
        {{ name }}:
          sku: 
            -
              name: ''
          name: '${var.{{ name }}_name}'
          # tags: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${azurerm_arm_relay_namespace.{{ name }}.id}'
      thub_id:
        value: '${azurerm_arm_relay_namespace.{{ name }}.id}'
      metric_id:
        value: '${azurerm_arm_relay_namespace.{{ name }}.metric_id}'
      primary_connection_string:
        value: '${azurerm_arm_relay_namespace.{{ name }}.primary_connection_string}'
      primary_key:
        value: '${azurerm_arm_relay_namespace.{{ name }}.primary_key}'
      secondary_connection_string:
        value: '${azurerm_arm_relay_namespace.{{ name }}.secondary_connection_string}'
      secondary_key:
        value: '${azurerm_arm_relay_namespace.{{ name }}.secondary_key}'
