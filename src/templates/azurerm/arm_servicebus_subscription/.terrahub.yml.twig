  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_servicebus_subscription:
        {{ name }}:
          max_delivery_count: var.{{ name }}_max_delivery_count
          name: var.{{ name }}_name
          namespace_name: var.{{ name }}_namespace_name
          topic_name: var.{{ name }}_topic_name
          # default_message_ttl: ''
          # lock_duration: ''
          # enable_batched_operations: false
          # forward_to: ''
          # auto_delete_on_idle: ''
          # dead_lettering_on_message_expiration: false
          # requires_session: false
          # dead_lettering_on_filter_evaluation_exceptions: false
    variable:
      {{ name }}_max_delivery_count:
        type: int
      {{ name }}_name:
        type: string
      {{ name }}_namespace_name:
        type: string
      {{ name }}_topic_name:
        type: string
    output:
      id:
        value: azurerm_arm_servicebus_subscription.{{ name }}.id
      thub_id:
        value: azurerm_arm_servicebus_subscription.{{ name }}.id
      default_message_ttl:
        value: azurerm_arm_servicebus_subscription.{{ name }}.default_message_ttl
      lock_duration:
        value: azurerm_arm_servicebus_subscription.{{ name }}.lock_duration
      auto_delete_on_idle:
        value: azurerm_arm_servicebus_subscription.{{ name }}.auto_delete_on_idle
