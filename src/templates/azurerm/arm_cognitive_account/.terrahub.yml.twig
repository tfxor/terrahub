  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_cognitive_account:
        {{ name }}:
          kind: '${var.{{ name }}_kind}'
          name: '${var.{{ name }}_name}'
          sku:
            -
              name: ''
              tier: ''
          # tags:
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_kind:
        type: 'string'
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${azurerm_arm_cognitive_account.{{ name }}.id}'
      thub_id:
        value: '${azurerm_arm_cognitive_account.{{ name }}.id}'
      endpoint:
        value: '${azurerm_arm_cognitive_account.{{ name }}.endpoint}'
      primary_access_key:
        value: '${azurerm_arm_cognitive_account.{{ name }}.primary_access_key}'
      secondary_access_key:
        value: '${azurerm_arm_cognitive_account.{{ name }}.secondary_access_key}'
