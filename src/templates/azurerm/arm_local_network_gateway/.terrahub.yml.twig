  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_local_network_gateway:
        {{ name }}:
          gateway_address: '${var.{{ name }}_gateway_address}'
          address_space: '${var.{{ name }}_address_space}'
          name: '${var.{{ name }}_name}'
          # bgp_settings: 
          #   -
          #     asn: 0
          #     bgp_peering_address: ''
          #     peer_weight: 0
          # tags: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_gateway_address:
        type: 'string'
      {{ name }}_address_space:
        type: 'list'
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${azurerm_arm_local_network_gateway.{{ name }}.id}'
      thub_id:
        value: '${azurerm_arm_local_network_gateway.{{ name }}.id}'
