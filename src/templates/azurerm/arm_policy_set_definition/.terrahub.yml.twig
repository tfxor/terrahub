  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_policy_set_definition:
        {{ name }}:
          policy_type: '${var.{{ name }}_policy_type}'
          display_name: '${var.{{ name }}_display_name}'
          name: '${var.{{ name }}_name}'
          # management_group_id: ''
          # description: ''
          # metadata: ''
          # parameters: ''
          # policy_definitions: ''
    variable:
      {{ name }}_policy_type:
        type: 'string'
      {{ name }}_display_name:
        type: 'string'
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${azurerm_arm_policy_set_definition.{{ name }}.id}'
      thub_id:
        value: '${azurerm_arm_policy_set_definition.{{ name }}.id}'
