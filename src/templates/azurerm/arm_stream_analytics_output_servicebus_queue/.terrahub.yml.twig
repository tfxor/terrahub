  template:
    # terraform:
    #   backend:
    #     storage_account_name: 'abcd1234'
    #     container_name: 'tfstate'
    #     key: 'prod.terraform.tfstate'
    resource:
      azurerm_arm_stream_analytics_output_servicebus_queue:
        {{ name }}:
          shared_access_policy_key: var.{{ name }}_shared_access_policy_key
          shared_access_policy_name: var.{{ name }}_shared_access_policy_name
          name: var.{{ name }}_name
          stream_analytics_job_name: var.{{ name }}_stream_analytics_job_name
          queue_name: var.{{ name }}_queue_name
          servicebus_namespace: var.{{ name }}_servicebus_namespace
    variable:
      {{ name }}_shared_access_policy_key:
        type: string
      {{ name }}_shared_access_policy_name:
        type: string
      {{ name }}_name:
        type: string
      {{ name }}_stream_analytics_job_name:
        type: string
      {{ name }}_queue_name:
        type: string
      {{ name }}_servicebus_namespace:
        type: string
    output:
      id:
        value: azurerm_arm_stream_analytics_output_servicebus_queue.{{ name }}.id
      thub_id:
        value: azurerm_arm_stream_analytics_output_servicebus_queue.{{ name }}.id
