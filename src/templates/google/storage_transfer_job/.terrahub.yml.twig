  template:
    # terraform:
    #   backend:
    #     bucket: 'tf-state-prod'
    #     prefix: 'terraform/state'
    resource:
      google_storage_transfer_job:
        {{ name }}:
          list_url: var.{{ name }}_list_url
    variable:
      {{ name }}_list_url:
        type: string
    output:
      id:
        value: google_storage_transfer_job.{{ name }}.id
      thub_id:
        value: google_storage_transfer_job.{{ name }}.id
