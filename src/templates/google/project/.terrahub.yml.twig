  template:
    # terraform:
    #   backend:
    #     bucket: 'tf-state-prod'
    #     prefix: 'terraform/state'
    resource:
      google_project:
        {{ name }}:
          # split_health_checks: false
    output:
      id:
        value: google_project.{{ name }}.id
      thub_id:
        value: google_project.{{ name }}.id
