  template:
    # terraform:
    #   backend:
    #     bucket: 'tf-state-prod'
    #     prefix: 'terraform/state'
    resource:
      google_dataflow_job:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          template_gcs_path: '${var.{{ name }}_template_gcs_path}'
          temp_gcs_location: '${var.{{ name }}_temp_gcs_location}'
          # zone: ''
          # region: ''
          # max_workers: 0
          # parameters:
          #   '[TO_BE_REPLACED]': ''
          # project: ''
    variable:
      {{ name }}_name:
        type: 'string'
      {{ name }}_template_gcs_path:
        type: 'string'
      {{ name }}_temp_gcs_location:
        type: 'string'
    output:
      id:
        value: '${google_dataflow_job.{{ name }}.id}'
      thub_id:
        value: '${google_dataflow_job.{{ name }}.id}'
      state:
        value: '${google_dataflow_job.{{ name }}.state}'
