  template:
    # terraform:
    #   backend:
    #     bucket: 'tf-state-prod'
    #     prefix: 'terraform/state'
    resource:
      google_compute_project_metadata_item:
        {{ name }}:
          key: var.{{ name }}_key
          value: var.{{ name }}_value
          # project: ''
    variable:
      {{ name }}_key:
        type: string
      {{ name }}_value:
        type: string
    output:
      id:
        value: google_compute_project_metadata_item.{{ name }}.id
      thub_id:
        value: google_compute_project_metadata_item.{{ name }}.id
      project:
        value: google_compute_project_metadata_item.{{ name }}.project
