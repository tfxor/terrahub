  template:
    # terraform:
    #   backend:
    #     bucket: 'tf-state-prod'
    #     prefix: 'terraform/state'
    resource:
      google_project_iam_policy:
        {{ name }}:
          project: var.{{ name }}_project
          policy_data: var.{{ name }}_policy_data
          # authoritative: false
          # disable_project: false
    variable:
      {{ name }}_project:
        type: string
      {{ name }}_policy_data:
        type: string
    output:
      id:
        value: google_project_iam_policy.{{ name }}.id
      thub_id:
        value: google_project_iam_policy.{{ name }}.id
      restore_policy:
        value: google_project_iam_policy.{{ name }}.restore_policy
      etag:
        value: google_project_iam_policy.{{ name }}.etag
