  template:
    # terraform:
    #   backend:
    #     bucket: 'tf-state-prod'
    #     prefix: 'terraform/state'
    resource:
      google_runtimeconfig_variable:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          parent: '${var.{{ name }}_parent}'
          # project: ''
          # value: ''
          # text: ''
    variable:
      {{ name }}_name:
        type: 'string'
      {{ name }}_parent:
        type: 'string'
    output:
      id:
        value: '${google_runtimeconfig_variable.{{ name }}.id}'
      thub_id:
        value: '${google_runtimeconfig_variable.{{ name }}.id}'
      project:
        value: '${google_runtimeconfig_variable.{{ name }}.project}'
      update_time:
        value: '${google_runtimeconfig_variable.{{ name }}.update_time}'
