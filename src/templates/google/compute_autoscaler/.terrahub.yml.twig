  template:
    # terraform:
    #   backend:
    #     bucket: 'tf-state-prod'
    #     prefix: 'terraform/state'
    resource:
      google_compute_autoscaler:
        {{ name }}:
          autoscaling_policy:
            -
              max_replicas: 0
              min_replicas: 0
          #     cooldown_period: 0
          #     cpu_utilization: 
          #       -
          #         target: ''
          name: '${var.{{ name }}_name}'
          target: '${var.{{ name }}_target}'
          # description: ''
          # metric: 
          #   -
          #     type: ''
          #     name: ''
          #     target: ''
          # zone: ''
          # project: ''
    variable:
      {{ name }}_name:
        type: 'string'
      {{ name }}_target:
        type: 'string'
    output:
      id:
        value: '${google_compute_autoscaler.{{ name }}.id}'
      thub_id:
        value: '${google_compute_autoscaler.{{ name }}.id}'
      creation_timestamp:
        value: '${google_compute_autoscaler.{{ name }}.creation_timestamp}'
      zone:
        value: '${google_compute_autoscaler.{{ name }}.zone}'
      project:
        value: '${google_compute_autoscaler.{{ name }}.project}'
      self_link:
        value: '${google_compute_autoscaler.{{ name }}.self_link}'
