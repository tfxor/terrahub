  template:
    # terraform:
    #   backend:
    #     bucket: 'tf-state-prod'
    #     prefix: 'terraform/state'
    resource:
      google_spanner_database:
        {{ name }}:
          instance: '${var.{{ name }}_instance}'
          name: '${var.{{ name }}_name}'
          # project: ''
          # ddl: []
    variable:
      {{ name }}_instance:
        type: 'string'
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${google_spanner_database.{{ name }}.id}'
      thub_id:
        value: '${google_spanner_database.{{ name }}.id}'
      project:
        value: '${google_spanner_database.{{ name }}.project}'
      state:
        value: '${google_spanner_database.{{ name }}.state}'
