  template:
    # terraform:
    #   backend:
    #     bucket: 'tf-state-prod'
    #     prefix: 'terraform/state'
    resource:
      google_bigtable_table:
        {{ name }}:
          instance_name: '${var.{{ name }}_instance_name}'
          name: '${var.{{ name }}_name}'
          # column_family: 
          #   family: ''
          # split_keys: []
          # project: ''
    variable:
      {{ name }}_instance_name:
        type: 'string'
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${google_bigtable_table.{{ name }}.id}'
      thub_id:
        value: '${google_bigtable_table.{{ name }}.id}'
      project:
        value: '${google_bigtable_table.{{ name }}.project}'
