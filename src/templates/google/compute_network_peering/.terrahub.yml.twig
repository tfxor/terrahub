  template:
    # terraform:
    #   backend:
    #     bucket: 'tf-state-prod'
    #     prefix: 'terraform/state'
    resource:
      google_compute_network_peering:
        {{ name }}:
          network: var.{{ name }}_network
          peer_network: var.{{ name }}_peer_network
          name: var.{{ name }}_name
          # auto_create_routes: false
    variable:
      {{ name }}_network:
        type: string
      {{ name }}_peer_network:
        type: string
      {{ name }}_name:
        type: string
    output:
      id:
        value: google_compute_network_peering.{{ name }}.id
      thub_id:
        value: google_compute_network_peering.{{ name }}.id
      state:
        value: google_compute_network_peering.{{ name }}.state
      state_details:
        value: google_compute_network_peering.{{ name }}.state_details
