  template:
    # terraform:
    #   backend:
    #     bucket: 'tf-state-prod'
    #     prefix: 'terraform/state'
    resource:
      google_compute_address:
        {{ name }}:
          name: var.{{ name }}_name
          # network_tier: ''
          # address: ''
          # address_type: ''
          # description: ''
          # region: ''
          # subnetwork: ''
          # project: ''
    variable:
      {{ name }}_name:
        type: string
    output:
      id:
        value: google_compute_address.{{ name }}.id
      thub_id:
        value: google_compute_address.{{ name }}.id
      network_tier:
        value: google_compute_address.{{ name }}.network_tier
      users:
        value: google_compute_address.{{ name }}.users
      self_link:
        value: google_compute_address.{{ name }}.self_link
      address:
        value: google_compute_address.{{ name }}.address
      region:
        value: google_compute_address.{{ name }}.region
      subnetwork:
        value: google_compute_address.{{ name }}.subnetwork
      creation_timestamp:
        value: google_compute_address.{{ name }}.creation_timestamp
      project:
        value: google_compute_address.{{ name }}.project
