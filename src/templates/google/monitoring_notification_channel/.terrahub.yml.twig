  template:
    # terraform:
    #   backend:
    #     bucket: 'tf-state-prod'
    #     prefix: 'terraform/state'
    resource:
      google_monitoring_notification_channel:
        {{ name }}:
          type: '${var.{{ name }}_type}'
          display_name: '${var.{{ name }}_display_name}'
          # enabled: false
          # labels:
          #   '[TO_BE_REPLACED]': ''
          # user_labels:
          #   '[TO_BE_REPLACED]': ''
          # project: ''
          # description: ''
    variable:
      {{ name }}_type:
        type: 'string'
      {{ name }}_display_name:
        type: 'string'
    output:
      id:
        value: '${google_monitoring_notification_channel.{{ name }}.id}'
      thub_id:
        value: '${google_monitoring_notification_channel.{{ name }}.id}'
      project:
        value: '${google_monitoring_notification_channel.{{ name }}.project}'
      name:
        value: '${google_monitoring_notification_channel.{{ name }}.name}'
      verification_status:
        value: '${google_monitoring_notification_channel.{{ name }}.verification_status}'
