  template:
    # terraform:
    #   backend:
    #     bucket: 'tf-state-prod'
    #     prefix: 'terraform/state'
    resource:
      google_compute_target_instance:
        {{ name }}:
          instance: var.{{ name }}_instance
          name: var.{{ name }}_name
          # project: ''
          # description: ''
          # nat_policy: ''
          # zone: ''
    variable:
      {{ name }}_instance:
        type: string
      {{ name }}_name:
        type: string
    output:
      id:
        value: google_compute_target_instance.{{ name }}.id
      thub_id:
        value: google_compute_target_instance.{{ name }}.id
      project:
        value: google_compute_target_instance.{{ name }}.project
      self_link:
        value: google_compute_target_instance.{{ name }}.self_link
      zone:
        value: google_compute_target_instance.{{ name }}.zone
      creation_timestamp:
        value: google_compute_target_instance.{{ name }}.creation_timestamp
