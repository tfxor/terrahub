  template:
    # terraform:
    #   backend:
    #     bucket: 'tf-state-prod'
    #     prefix: 'terraform/state'
    resource:
      google_compute_backend_service:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          health_checks: '${var.{{ name }}_health_checks}'
          # iap: 
          #   -
          #     oauth2_client_id: ''
          #     oauth2_client_secret: ''
          # backend: 
          #   max_utilization:''
          #   group:''
          #   capacity_scaler:''
          #   max_connections:0
          #   max_connections_per_instance:0
          #   balancing_mode:''
          #   description:''
          #   max_rate:0
          #   max_rate_per_instance:''
          # protocol: ''
          # session_affinity: ''
          # timeout_sec: 0
          # custom_request_headers: []
          # security_policy: ''
          # affinity_cookie_ttl_sec: 0
          # connection_draining_timeout_sec: 0
          # cdn_policy: 
          #   -
          #     cache_key_policy: 
          #       -
          #         include_query_string: false
          #         query_string_blacklist: 
          #         query_string_whitelist: 
          #         include_host: false
          #         include_protocol: false
          # description: ''
          # enable_cdn: false
          # port_name: ''
          # project: ''
          # region: ''
    variable:
      {{ name }}_name:
        type: 'string'
      {{ name }}_health_checks:
        type: 'set'
    output:
      id:
        value: '${google_compute_backend_service.{{ name }}.id}'
      thub_id:
        value: '${google_compute_backend_service.{{ name }}.id}'
      protocol:
        value: '${google_compute_backend_service.{{ name }}.protocol}'
      self_link:
        value: '${google_compute_backend_service.{{ name }}.self_link}'
      session_affinity:
        value: '${google_compute_backend_service.{{ name }}.session_affinity}'
      timeout_sec:
        value: '${google_compute_backend_service.{{ name }}.timeout_sec}'
      cdn_policy:
        value: '${google_compute_backend_service.{{ name }}.cdn_policy}'
      fingerprint:
        value: '${google_compute_backend_service.{{ name }}.fingerprint}'
      port_name:
        value: '${google_compute_backend_service.{{ name }}.port_name}'
      project:
        value: '${google_compute_backend_service.{{ name }}.project}'
