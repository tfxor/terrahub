  template:
    # terraform:
    #   backend:
    #     bucket: 'tf-state-prod'
    #     prefix: 'terraform/state'
    resource:
      google_compute_router_interface:
        {{ name }}:
          name: var.{{ name }}_name
          router: var.{{ name }}_router
          # project: ''
          # region: ''
          # vpn_tunnel: ''
          # interconnect_attachment: ''
          # ip_range: ''
    variable:
      {{ name }}_name:
        type: string
      {{ name }}_router:
        type: string
    output:
      id:
        value: google_compute_router_interface.{{ name }}.id
      thub_id:
        value: google_compute_router_interface.{{ name }}.id
      project:
        value: google_compute_router_interface.{{ name }}.project
      region:
        value: google_compute_router_interface.{{ name }}.region
