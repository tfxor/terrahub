  template:
    # terraform:
    #   backend:
    #     bucket: 'tf-state-prod'
    #     prefix: 'terraform/state'
    resource:
      google_source_repo_repository:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          # project: ''
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${google_source_repo_repository.{{ name }}.id}'
      thub_id:
        value: '${google_source_repo_repository.{{ name }}.id}'
      project:
        value: '${google_source_repo_repository.{{ name }}.project}'
      size:
        value: '${google_source_repo_repository.{{ name }}.size}'
      url:
        value: '${google_source_repo_repository.{{ name }}.url}'
