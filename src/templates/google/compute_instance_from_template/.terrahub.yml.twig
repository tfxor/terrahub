  template:
    # terraform:
    #   backend:
    #     bucket: 'tf-state-prod'
    #     prefix: 'terraform/state'
    resource:
      google_compute_instance_from_template:
        {{ name }}:
    output:
      id:
        value: '${google_compute_instance_from_template.{{ name }}.id}'
      thub_id:
        value: '${google_compute_instance_from_template.{{ name }}.id}'
