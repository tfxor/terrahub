  template:
    # terraform:
    #   backend:
    #     bucket: 'tf-state-prod'
    #     prefix: 'terraform/state'
    resource:
      google_compute_backend_bucket:
        {{ name }}:
          bucket_name: '${var.{{ name }}_bucket_name}'
          name: '${var.{{ name }}_name}'
          # project: ''
          # description: ''
          # enable_cdn: false
    variable:
      {{ name }}_bucket_name:
        type: 'string'
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${google_compute_backend_bucket.{{ name }}.id}'
      thub_id:
        value: '${google_compute_backend_bucket.{{ name }}.id}'
      project:
        value: '${google_compute_backend_bucket.{{ name }}.project}'
      self_link:
        value: '${google_compute_backend_bucket.{{ name }}.self_link}'
      creation_timestamp:
        value: '${google_compute_backend_bucket.{{ name }}.creation_timestamp}'
