  template:
    # terraform:
    #   backend:
    #     bucket: 'tf-state-prod'
    #     prefix: 'terraform/state'
    resource:
      google_composer_environment:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          # labels:
          #   '[TO_BE_REPLACED]': ''
          # region: ''
          # project: ''
          # config:
          #   -
          #     node_count: 0
          #     node_config:
          #       -
          #         zone: ''
          #         tags:
          #         machine_type: ''
          #         network: ''
          #         subnetwork: ''
          #         disk_size_gb: 0
          #         oauth_scopes:
          #         service_account: ''
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${google_composer_environment.{{ name }}.id}'
      thub_id:
        value: '${google_composer_environment.{{ name }}.id}'
      dag_gcs_prefix:
        value: '${google_composer_environment.{{ name }}.dag_gcs_prefix}'
      gke_cluster:
        value: '${google_composer_environment.{{ name }}.gke_cluster}'
      project:
        value: '${google_composer_environment.{{ name }}.project}'
      config:
        value: '${google_composer_environment.{{ name }}.config}'
      airflow_uri:
        value: '${google_composer_environment.{{ name }}.airflow_uri}'
