  template:
    # terraform:
    #   backend:
    #     bucket: 'tf-state-prod'
    #     prefix: 'terraform/state'
    resource:
      google_runtimeconfig_config:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          # description: ''
          # project: ''
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${google_runtimeconfig_config.{{ name }}.id}'
      thub_id:
        value: '${google_runtimeconfig_config.{{ name }}.id}'
      project:
        value: '${google_runtimeconfig_config.{{ name }}.project}'
