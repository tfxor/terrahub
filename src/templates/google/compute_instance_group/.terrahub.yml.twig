  template:
    # terraform:
    #   backend:
    #     bucket: 'tf-state-prod'
    #     prefix: 'terraform/state'
    resource:
      google_compute_instance_group:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          # project: ''
          # zone: ''
          # description: ''
          # instances: []
          # named_port: 
          #   -
          #     name: ''
          #     port: 0
          # network: ''
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${google_compute_instance_group.{{ name }}.id}'
      thub_id:
        value: '${google_compute_instance_group.{{ name }}.id}'
      project:
        value: '${google_compute_instance_group.{{ name }}.project}'
      self_link:
        value: '${google_compute_instance_group.{{ name }}.self_link}'
      size:
        value: '${google_compute_instance_group.{{ name }}.size}'
      zone:
        value: '${google_compute_instance_group.{{ name }}.zone}'
      instances:
        value: '${google_compute_instance_group.{{ name }}.instances}'
      network:
        value: '${google_compute_instance_group.{{ name }}.network}'
