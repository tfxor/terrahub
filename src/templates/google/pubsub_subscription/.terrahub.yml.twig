  template:
    # terraform:
    #   backend:
    #     bucket: 'tf-state-prod'
    #     prefix: 'terraform/state'
    resource:
      google_pubsub_subscription:
        {{ name }}:
          name: var.{{ name }}_name
          topic: var.{{ name }}_topic
          # labels: 
          #   '[TO_BE_REPLACED]': ''
          # message_retention_duration: ''
          # expiration_policy: 
          #   -
          #     ttl: ''
          # push_config: 
          #   -
          #     push_endpoint: ''
          #     attributes: 
          #       '[TO_BE_REPLACED]': ''
          # retain_acked_messages: false
          # project: ''
          # ack_deadline_seconds: 0
    variable:
      {{ name }}_name:
        type: string
      {{ name }}_topic:
        type: string
    output:
      id:
        value: google_pubsub_subscription.{{ name }}.id
      thub_id:
        value: google_pubsub_subscription.{{ name }}.id
      expiration_policy:
        value: google_pubsub_subscription.{{ name }}.expiration_policy
      path:
        value: google_pubsub_subscription.{{ name }}.path
      project:
        value: google_pubsub_subscription.{{ name }}.project
      ack_deadline_seconds:
        value: google_pubsub_subscription.{{ name }}.ack_deadline_seconds
