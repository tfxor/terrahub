  template:
    # terraform:
    #   backend:
    #     bucket: 'tf-state-prod'
    #     prefix: 'terraform/state'
    resource:
      google_project_organization_policy:
        {{ name }}:
    output:
      id:
        value: '${google_project_organization_policy.{{ name }}.id}'
      thub_id:
        value: '${google_project_organization_policy.{{ name }}.id}'
