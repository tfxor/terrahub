  template:
    # terraform:
    #   backend:
    #     bucket: 'tf-state-prod'
    #     prefix: 'terraform/state'
    resource:
      google_kms_crypto_key:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          key_ring: '${var.{{ name }}_key_ring}'
          # rotation_period: ''
          # version_template:
          #   -
          #     algorithm: ''
          #     protection_level: ''
    variable:
      {{ name }}_name:
        type: 'string'
      {{ name }}_key_ring:
        type: 'string'
    output:
      id:
        value: '${google_kms_crypto_key.{{ name }}.id}'
      thub_id:
        value: '${google_kms_crypto_key.{{ name }}.id}'
      self_link:
        value: '${google_kms_crypto_key.{{ name }}.self_link}'
      version_template:
        value: '${google_kms_crypto_key.{{ name }}.version_template}'
