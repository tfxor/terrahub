{
  "project": {
    "name": "{{ name }}",
    "code": "{{ code }}",
    "provider": "{{ provider }}",
    "include": ["./"],
    "ignore": [
      "**/.terraform/*",
      "**/node_modules/*"
    ]
  },
  "terraform": {
    "varFile": ["default.tfvars"]
  }
}
