{
  "project": {
    "name": "{{ name }}",
    "code": "{{ code }}",
    "provider": "{{ provider }}",
    "include": ["."],
    "exclude": [
      "**/.terraform/*",
      "**/node_modules/*"
    ]
  },
  "terraform": {
    "varFile": ["default.tfvars"]
  }
}
