{
  "component": {
    "name": "{{ name }}",
    "mapping": ["."]
    {% if dependsOn %}
    ,"dependsOn": ["{{ dependsOn|join('","') }}"]
    {% endif %}
  }
  {% if data != '' %}
,{{ data }}
  {% endif %}
}
