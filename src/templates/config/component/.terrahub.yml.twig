## local config
component:
  name: '{{ name }}'
  mapping:
    - '.'
{% if dependsOn %}
  dependsOn:
{% for dep in dependsOn %}
    - '{{ dep }}'
{% endfor %}
{% endif %}
{% if data %}
{{ data }}
{% endif %}