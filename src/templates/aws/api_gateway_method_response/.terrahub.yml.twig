  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_api_gateway_method_response:
        {{ name }}:
          http_method: var.{{ name }}_http_method
          status_code: var.{{ name }}_status_code
          rest_api_id: var.{{ name }}_rest_api_id
          resource_id: var.{{ name }}_resource_id
          # response_models: 
          #   '[TO_BE_REPLACED]': ''
          # response_parameters: 
          #   '[TO_BE_REPLACED]': ''
          # response_parameters_in_json: ''
    variable:
      {{ name }}_http_method:
        type: string
      {{ name }}_status_code:
        type: string
      {{ name }}_rest_api_id:
        type: string
      {{ name }}_resource_id:
        type: string
    output:
      id:
        value: aws_api_gateway_method_response.{{ name }}.id
      thub_id:
        value: aws_api_gateway_method_response.{{ name }}.id
