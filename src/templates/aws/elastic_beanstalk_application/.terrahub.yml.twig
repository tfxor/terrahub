  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_elastic_beanstalk_application:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          # description: ''
          # appversion_lifecycle: 
          #   -
          #     service_role: ''
          #     max_count: 0
          #     delete_source_from_s3: false
          #     max_age_in_days: 0
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_elastic_beanstalk_application.{{ name }}.id}'
      thub_id:
        value: '${aws_elastic_beanstalk_application.{{ name }}.id}'
