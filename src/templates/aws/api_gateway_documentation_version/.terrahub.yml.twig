  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_api_gateway_documentation_version:
        {{ name }}:
          version: '${var.{{ name }}_version}'
          rest_api_id: '${var.{{ name }}_rest_api_id}'
          # description: ''
    variable:
      {{ name }}_version:
        type: 'string'
      {{ name }}_rest_api_id:
        type: 'string'
    output:
      id:
        value: '${aws_api_gateway_documentation_version.{{ name }}.id}'
      thub_id:
        value: '${aws_api_gateway_documentation_version.{{ name }}.id}'
