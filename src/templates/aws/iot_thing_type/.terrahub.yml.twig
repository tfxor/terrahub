  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_iot_thing_type:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          # properties: 
          #   -
          #     description: ''
          #     searchable_attributes: 
          # deprecated: false
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_iot_thing_type.{{ name }}.id}'
      thub_id:
        value: '${aws_iot_thing_type.{{ name }}.id}'
      arn:
        value: '${aws_iot_thing_type.{{ name }}.arn}'
