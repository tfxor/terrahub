  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_simpledb_domain:
        {{ name }}:
          name: '${var.{{ name }}_name}'
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_simpledb_domain.{{ name }}.id}'
      thub_id:
        value: '${aws_simpledb_domain.{{ name }}.id}'
