  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_secretsmanager_secret:
        {{ name }}:
          # description: ''
          # name_prefix: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
          # rotation_lambda_arn: ''
          # rotation_rules: 
          #   -
          #     automatically_after_days: 0
          # kms_key_id: ''
          # name: ''
          # policy: ''
          # recovery_window_in_days: 0
    output:
      id:
        value: '${aws_secretsmanager_secret.{{ name }}.id}'
      thub_id:
        value: '${aws_secretsmanager_secret.{{ name }}.id}'
      name_prefix:
        value: '${aws_secretsmanager_secret.{{ name }}.name_prefix}'
      arn:
        value: '${aws_secretsmanager_secret.{{ name }}.arn}'
      name:
        value: '${aws_secretsmanager_secret.{{ name }}.name}'
      rotation_enabled:
        value: '${aws_secretsmanager_secret.{{ name }}.rotation_enabled}'
