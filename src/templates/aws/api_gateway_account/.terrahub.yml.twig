  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_api_gateway_account:
        {{ name }}:
          # cloudwatch_role_arn: ''
    output:
      id:
        value: aws_api_gateway_account.{{ name }}.id
      thub_id:
        value: aws_api_gateway_account.{{ name }}.id
      throttle_settings:
        value: aws_api_gateway_account.{{ name }}.throttle_settings
