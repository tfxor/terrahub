  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_cognito_identity_provider:
        {{ name }}:
          provider_name: '${var.{{ name }}_provider_name}'
          provider_type: '${var.{{ name }}_provider_type}'
          user_pool_id: '${var.{{ name }}_user_pool_id}'
          provider_details: '${var.{{ name }}_provider_details}'
          # attribute_mapping: 
          #   '[TO_BE_REPLACED]': ''
          # idp_identifiers: []
    variable:
      {{ name }}_provider_name:
        type: 'string'
      {{ name }}_provider_type:
        type: 'string'
      {{ name }}_user_pool_id:
        type: 'string'
      {{ name }}_provider_details:
        type: 'map'
    output:
      id:
        value: '${aws_cognito_identity_provider.{{ name }}.id}'
      thub_id:
        value: '${aws_cognito_identity_provider.{{ name }}.id}'
