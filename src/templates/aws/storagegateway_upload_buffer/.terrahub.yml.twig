  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_storagegateway_upload_buffer:
        {{ name }}:
          disk_id: var.{{ name }}_disk_id
          gateway_arn: var.{{ name }}_gateway_arn
    variable:
      {{ name }}_disk_id:
        type: string
      {{ name }}_gateway_arn:
        type: string
    output:
      id:
        value: aws_storagegateway_upload_buffer.{{ name }}.id
      thub_id:
        value: aws_storagegateway_upload_buffer.{{ name }}.id
