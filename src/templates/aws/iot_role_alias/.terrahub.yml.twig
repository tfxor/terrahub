  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_iot_role_alias:
        {{ name }}:
          alias: var.{{ name }}_alias
          role_arn: var.{{ name }}_role_arn
          # credential_duration: 0
    variable:
      {{ name }}_alias:
        type: string
      {{ name }}_role_arn:
        type: string
    output:
      id:
        value: aws_iot_role_alias.{{ name }}.id
      thub_id:
        value: aws_iot_role_alias.{{ name }}.id
      arn:
        value: aws_iot_role_alias.{{ name }}.arn
