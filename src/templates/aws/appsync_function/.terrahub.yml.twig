  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_appsync_function:
        {{ name }}:
          data_source: var.{{ name }}_data_source
          name: var.{{ name }}_name
          api_id: var.{{ name }}_api_id
          request_mapping_template: var.{{ name }}_request_mapping_template
          response_mapping_template: var.{{ name }}_response_mapping_template
          # description: ''
          # function_version: ''
    variable:
      {{ name }}_data_source:
        type: string
      {{ name }}_name:
        type: string
      {{ name }}_api_id:
        type: string
      {{ name }}_request_mapping_template:
        type: string
      {{ name }}_response_mapping_template:
        type: string
    output:
      id:
        value: aws_appsync_function.{{ name }}.id
      thub_id:
        value: aws_appsync_function.{{ name }}.id
      arn:
        value: aws_appsync_function.{{ name }}.arn
      function_id:
        value: aws_appsync_function.{{ name }}.function_id
