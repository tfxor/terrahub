  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_glue_trigger:
        {{ name }}:
          type: '${var.{{ name }}_type}'
          actions: 
            -
              job_name: ''
          #     arguments: 
          #       '[TO_BE_REPLACED]': ''
          #     timeout: 0
          name: '${var.{{ name }}_name}'
          # schedule: ''
          # description: ''
          # enabled: false
          # predicate: 
          #   -
          #     conditions: 
          #       -
          #         job_name: ''
          #         state: ''
          #         logical_operator: ''
    variable:
      {{ name }}_type:
        type: 'string'
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_glue_trigger.{{ name }}.id}'
      thub_id:
        value: '${aws_glue_trigger.{{ name }}.id}'
