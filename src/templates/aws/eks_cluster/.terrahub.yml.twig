  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_eks_cluster:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          vpc_config: 
            -
              subnet_ids: []
          #     security_group_ids: 
          role_arn: '${var.{{ name }}_role_arn}'
          # version: ''
    variable:
      {{ name }}_name:
        type: 'string'
      {{ name }}_role_arn:
        type: 'string'
    output:
      id:
        value: '${aws_eks_cluster.{{ name }}.id}'
      thub_id:
        value: '${aws_eks_cluster.{{ name }}.id}'
      created_at:
        value: '${aws_eks_cluster.{{ name }}.created_at}'
      platform_version:
        value: '${aws_eks_cluster.{{ name }}.platform_version}'
      arn:
        value: '${aws_eks_cluster.{{ name }}.arn}'
      certificate_authority:
        value: '${aws_eks_cluster.{{ name }}.certificate_authority}'
      version:
        value: '${aws_eks_cluster.{{ name }}.version}'
      endpoint:
        value: '${aws_eks_cluster.{{ name }}.endpoint}'
