  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_ses_email_identity:
        {{ name }}:
          email: var.{{ name }}_email
    variable:
      {{ name }}_email:
        type: string
    output:
      id:
        value: aws_ses_email_identity.{{ name }}.id
      thub_id:
        value: aws_ses_email_identity.{{ name }}.id
      arn:
        value: aws_ses_email_identity.{{ name }}.arn
