  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_api_gateway_authorizer:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          rest_api_id: '${var.{{ name }}_rest_api_id}'
          # authorizer_uri: ''
          # identity_source: ''
          # type: ''
          # authorizer_credentials: ''
          # identity_validation_expression: ''
          # authorizer_result_ttl_in_seconds: 0
          # provider_arns: []
    variable:
      {{ name }}_name:
        type: 'string'
      {{ name }}_rest_api_id:
        type: 'string'
    output:
      id:
        value: '${aws_api_gateway_authorizer.{{ name }}.id}'
      thub_id:
        value: '${aws_api_gateway_authorizer.{{ name }}.id}'
