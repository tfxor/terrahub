  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_athena_named_query:
        {{ name }}:
          query: '${var.{{ name }}_query}'
          database: '${var.{{ name }}_database}'
          name: '${var.{{ name }}_name}'
          # description: ''
    variable:
      {{ name }}_query:
        type: 'string'
      {{ name }}_database:
        type: 'string'
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_athena_named_query.{{ name }}.id}'
      thub_id:
        value: '${aws_athena_named_query.{{ name }}.id}'
