  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_appsync_datasource:
        {{ name }}:
          api_id: '${var.{{ name }}_api_id}'
          name: '${var.{{ name }}_name}'
          type: '${var.{{ name }}_type}'
          # description: ''
          # lambda_config: 
          #   -
          #     function_arn: ''
          # service_role_arn: ''
          # dynamodb_config: 
          #   -
          #     table_name: ''
          #     region: ''
          #     use_caller_credentials: false
          # elasticsearch_config: 
          #   -
          #     endpoint: ''
          #     region: ''
          # http_config: 
          #   -
          #     endpoint: ''
    variable:
      {{ name }}_api_id:
        type: 'string'
      {{ name }}_name:
        type: 'string'
      {{ name }}_type:
        type: 'string'
    output:
      id:
        value: '${aws_appsync_datasource.{{ name }}.id}'
      thub_id:
        value: '${aws_appsync_datasource.{{ name }}.id}'
      arn:
        value: '${aws_appsync_datasource.{{ name }}.arn}'
