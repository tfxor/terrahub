  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_emr_instance_group:
        {{ name }}:
          instance_type: var.{{ name }}_instance_type
          cluster_id: var.{{ name }}_cluster_id
          # instance_count: 0
          # name: ''
          # autoscaling_policy: ''
          # bid_price: ''
          # ebs_optimized: false
          # ebs_config: 
          #   type: ''
          #   size: 0
          #   volumes_per_instance: 0
          #   iops: 0
    variable:
      {{ name }}_instance_type:
        type: string
      {{ name }}_cluster_id:
        type: string
    output:
      id:
        value: aws_emr_instance_group.{{ name }}.id
      thub_id:
        value: aws_emr_instance_group.{{ name }}.id
      running_instance_count:
        value: aws_emr_instance_group.{{ name }}.running_instance_count
      status:
        value: aws_emr_instance_group.{{ name }}.status
      ebs_config:
        value: aws_emr_instance_group.{{ name }}.ebs_config
