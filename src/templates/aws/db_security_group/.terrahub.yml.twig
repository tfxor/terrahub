  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_db_security_group:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          ingress: 
          #   security_group_id: ''
          #   security_group_owner_id: ''
          #   cidr: ''
          #   security_group_name: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
          # description: ''
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_db_security_group.{{ name }}.id}'
      thub_id:
        value: '${aws_db_security_group.{{ name }}.id}'
      arn:
        value: '${aws_db_security_group.{{ name }}.arn}'
