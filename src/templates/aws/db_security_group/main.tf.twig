resource "aws_db_security_group" "{{ name }}" {
  name        = "${var.db_security_group_name}"
  description = "${var.db_security_group_description}"

  ingress {
    security_group_name = "${var.db_security_group_ingress_sg_name}"
  }

  tags = "${merge(var.default_tags, var.custom_tags)}"
}