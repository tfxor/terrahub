resource "aws_api_gateway_integration" "{{ name }}" {
  rest_api_id            = "${var.aws_api_gateway_rest_api_id}"
  resource_id            = "${var.aws_api_gateway_resource_id}"
  http_method            = "${var.aws_api_gateway_method_http_method}"
  type                   = "${var.api_gateway_integration_type}"
  # cache_key_parameters = ["${var.api_gateway_integration_cache_key_parameters}"]
  # cache_namespace      = "${var.api_gateway_integration_cache_key_cache_namespace"
}
