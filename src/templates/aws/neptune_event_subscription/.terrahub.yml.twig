  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_neptune_event_subscription:
        {{ name }}:
          sns_topic_arn: '${var.{{ name }}_sns_topic_arn}'
          # enabled: false
          # tags: 
          #   '[TO_BE_REPLACED]': ''
          # source_ids: []
          # source_type: ''
          # event_categories: []
          # name: ''
          # name_prefix: ''
    variable:
      {{ name }}_sns_topic_arn:
        type: 'string'
    output:
      id:
        value: '${aws_neptune_event_subscription.{{ name }}.id}'
      thub_id:
        value: '${aws_neptune_event_subscription.{{ name }}.id}'
      arn:
        value: '${aws_neptune_event_subscription.{{ name }}.arn}'
      customer_aws_id:
        value: '${aws_neptune_event_subscription.{{ name }}.customer_aws_id}'
      name:
        value: '${aws_neptune_event_subscription.{{ name }}.name}'
      name_prefix:
        value: '${aws_neptune_event_subscription.{{ name }}.name_prefix}'
