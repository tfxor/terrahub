  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_route53_query_log:
        {{ name }}:
          zone_id: '${var.{{ name }}_zone_id}'
          cloudwatch_log_group_arn: '${var.{{ name }}_cloudwatch_log_group_arn}'
    variable:
      {{ name }}_zone_id:
        type: 'string'
      {{ name }}_cloudwatch_log_group_arn:
        type: 'string'
    output:
      id:
        value: '${aws_route53_query_log.{{ name }}.id}'
      thub_id:
        value: '${aws_route53_query_log.{{ name }}.id}'
