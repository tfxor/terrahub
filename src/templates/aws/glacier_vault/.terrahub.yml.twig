  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_glacier_vault:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          # notification: 
          #   -
          #     events: []
          #     sns_topic: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
          # access_policy: ''
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_glacier_vault.{{ name }}.id}'
      thub_id:
        value: '${aws_glacier_vault.{{ name }}.id}'
      location:
        value: '${aws_glacier_vault.{{ name }}.location}'
      arn:
        value: '${aws_glacier_vault.{{ name }}.arn}'
