  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_ssm_parameter:
        {{ name }}:
          name: var.{{ name }}_name
          type: var.{{ name }}_type
          value: var.{{ name }}_value
          # tier: ''
          # key_id: ''
          # overwrite: false
          # tags: 
          #   '[TO_BE_REPLACED]': ''
          # description: ''
          # arn: ''
          # allowed_pattern: ''
    variable:
      {{ name }}_name:
        type: string
      {{ name }}_type:
        type: string
      {{ name }}_value:
        type: string
    output:
      id:
        value: aws_ssm_parameter.{{ name }}.id
      thub_id:
        value: aws_ssm_parameter.{{ name }}.id
      key_id:
        value: aws_ssm_parameter.{{ name }}.key_id
      version:
        value: aws_ssm_parameter.{{ name }}.version
      arn:
        value: aws_ssm_parameter.{{ name }}.arn
