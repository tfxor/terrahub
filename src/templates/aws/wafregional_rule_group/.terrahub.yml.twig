  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_wafregional_rule_group:
        {{ name }}:
          name: var.{{ name }}_name
          metric_name: var.{{ name }}_metric_name
          # activated_rule: 
          #   action: 
          #     -
          #       type: ''
    variable:
      {{ name }}_name:
        type: string
      {{ name }}_metric_name:
        type: string
    output:
      id:
        value: aws_wafregional_rule_group.{{ name }}.id
      thub_id:
        value: aws_wafregional_rule_group.{{ name }}.id
