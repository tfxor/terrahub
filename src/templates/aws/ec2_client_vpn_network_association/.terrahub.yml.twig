  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_ec2_client_vpn_network_association:
        {{ name }}:
          client_vpn_endpoint_id: var.{{ name }}_client_vpn_endpoint_id
          subnet_id: var.{{ name }}_subnet_id
    variable:
      {{ name }}_client_vpn_endpoint_id:
        type: string
      {{ name }}_subnet_id:
        type: string
    output:
      id:
        value: aws_ec2_client_vpn_network_association.{{ name }}.id
      thub_id:
        value: aws_ec2_client_vpn_network_association.{{ name }}.id
      vpc_id:
        value: aws_ec2_client_vpn_network_association.{{ name }}.vpc_id
      security_groups:
        value: aws_ec2_client_vpn_network_association.{{ name }}.security_groups
      status:
        value: aws_ec2_client_vpn_network_association.{{ name }}.status
