# Define list of variables to be output

output "cloudwatch_event_target_rule" {
  value = "${aws_cloudwatch_event_target.{{ name }}.rule}"
}

output "cloudwatch_event_target_arn" {
  value = "${aws_cloudwatch_event_target.{{ name }}.arn}"
}
