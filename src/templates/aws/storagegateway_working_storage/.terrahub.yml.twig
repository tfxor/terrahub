  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_storagegateway_working_storage:
        {{ name }}:
          gateway_arn: '${var.{{ name }}_gateway_arn}'
          disk_id: '${var.{{ name }}_disk_id}'
    variable:
      {{ name }}_gateway_arn:
        type: 'string'
      {{ name }}_disk_id:
        type: 'string'
    output:
      id:
        value: '${aws_storagegateway_working_storage.{{ name }}.id}'
      thub_id:
        value: '${aws_storagegateway_working_storage.{{ name }}.id}'
