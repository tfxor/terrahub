  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_dx_connection_association:
        {{ name }}:
          lag_id: '${var.{{ name }}_lag_id}'
          connection_id: '${var.{{ name }}_connection_id}'
    variable:
      {{ name }}_lag_id:
        type: 'string'
      {{ name }}_connection_id:
        type: 'string'
    output:
      id:
        value: '${aws_dx_connection_association.{{ name }}.id}'
      thub_id:
        value: '${aws_dx_connection_association.{{ name }}.id}'
