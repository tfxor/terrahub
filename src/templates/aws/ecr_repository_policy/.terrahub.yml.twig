  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_ecr_repository_policy:
        {{ name }}:
          policy: '${var.{{ name }}_policy}'
          repository: '${var.{{ name }}_repository}'
    variable:
      {{ name }}_policy:
        type: 'string'
      {{ name }}_repository:
        type: 'string'
    output:
      id:
        value: '${aws_ecr_repository_policy.{{ name }}.id}'
      thub_id:
        value: '${aws_ecr_repository_policy.{{ name }}.id}'
      registry_id:
        value: '${aws_ecr_repository_policy.{{ name }}.registry_id}'
