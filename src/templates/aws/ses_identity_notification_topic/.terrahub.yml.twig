  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_ses_identity_notification_topic:
        {{ name }}:
          notification_type: '${var.{{ name }}_notification_type}'
          identity: '${var.{{ name }}_identity}'
          # topic_arn: ''
    variable:
      {{ name }}_notification_type:
        type: 'string'
      {{ name }}_identity:
        type: 'string'
    output:
      id:
        value: '${aws_ses_identity_notification_topic.{{ name }}.id}'
      thub_id:
        value: '${aws_ses_identity_notification_topic.{{ name }}.id}'
