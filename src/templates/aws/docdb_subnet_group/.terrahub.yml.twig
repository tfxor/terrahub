  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_docdb_subnet_group:
        {{ name }}:
          subnet_ids: '${var.{{ name }}_subnet_ids}'
          # description: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
          # name: ''
          # name_prefix: ''
    variable:
      {{ name }}_subnet_ids:
        type: 'set'
    output:
      id:
        value: '${aws_docdb_subnet_group.{{ name }}.id}'
      thub_id:
        value: '${aws_docdb_subnet_group.{{ name }}.id}'
      arn:
        value: '${aws_docdb_subnet_group.{{ name }}.arn}'
      name:
        value: '${aws_docdb_subnet_group.{{ name }}.name}'
      name_prefix:
        value: '${aws_docdb_subnet_group.{{ name }}.name_prefix}'
