  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_vpc_dhcp_options_association:
        {{ name }}:
          vpc_id: '${var.{{ name }}_vpc_id}'
          dhcp_options_id: '${var.{{ name }}_dhcp_options_id}'
    variable:
      {{ name }}_vpc_id:
        type: 'string'
      {{ name }}_dhcp_options_id:
        type: 'string'
    output:
      id:
        value: '${aws_vpc_dhcp_options_association.{{ name }}.id}'
      thub_id:
        value: '${aws_vpc_dhcp_options_association.{{ name }}.id}'
