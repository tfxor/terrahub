  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_ssm_data_sync:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          s3_destination:
            -
              bucket_name: ''
              region: ''
          #     kms_key_arn: ''
          #     prefix: ''
          #     sync_format: ''
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_ssm_data_sync.{{ name }}.id}'
      thub_id:
        value: '${aws_ssm_data_sync.{{ name }}.id}'
