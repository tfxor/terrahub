  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_ssm_data_sync:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          s3_destination:
            -
              region: ''
              bucket_name: ''
          #     sync_format: ''
          #     kms_key_arn: ''
          #     prefix: ''
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_ssm_data_sync.{{ name }}.id}'
      thub_id:
        value: '${aws_ssm_data_sync.{{ name }}.id}'
