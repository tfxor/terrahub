  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_pinpoint_apns_sandbox_channel:
        {{ name }}:
          application_id: '${var.{{ name }}_application_id}'
          # certificate: ''
          # default_authentication_method: ''
          # token_key_id: ''
          # bundle_id: ''
          # enabled: false
          # private_key: ''
          # team_id: ''
          # token_key: ''
    variable:
      {{ name }}_application_id:
        type: 'string'
    output:
      id:
        value: '${aws_pinpoint_apns_sandbox_channel.{{ name }}.id}'
      thub_id:
        value: '${aws_pinpoint_apns_sandbox_channel.{{ name }}.id}'
