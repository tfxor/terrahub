  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_shield_protection:
        {{ name }}:
          name: var.{{ name }}_name
          resource_arn: var.{{ name }}_resource_arn
    variable:
      {{ name }}_name:
        type: string
      {{ name }}_resource_arn:
        type: string
    output:
      id:
        value: aws_shield_protection.{{ name }}.id
      thub_id:
        value: aws_shield_protection.{{ name }}.id
