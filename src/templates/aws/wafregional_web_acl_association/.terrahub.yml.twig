  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_wafregional_web_acl_association:
        {{ name }}:
          web_acl_id: '${var.{{ name }}_web_acl_id}'
          resource_arn: '${var.{{ name }}_resource_arn}'
    variable:
      {{ name }}_web_acl_id:
        type: 'string'
      {{ name }}_resource_arn:
        type: 'string'
    output:
      id:
        value: '${aws_wafregional_web_acl_association.{{ name }}.id}'
      thub_id:
        value: '${aws_wafregional_web_acl_association.{{ name }}.id}'
