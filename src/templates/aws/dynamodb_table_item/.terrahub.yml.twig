  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_dynamodb_table_item:
        {{ name }}:
          hash_key: '${var.{{ name }}_hash_key}'
          item: '${var.{{ name }}_item}'
          table_name: '${var.{{ name }}_table_name}'
          # range_key: ''
    variable:
      {{ name }}_hash_key:
        type: 'string'
      {{ name }}_item:
        type: 'string'
      {{ name }}_table_name:
        type: 'string'
    output:
      id:
        value: '${aws_dynamodb_table_item.{{ name }}.id}'
      thub_id:
        value: '${aws_dynamodb_table_item.{{ name }}.id}'
