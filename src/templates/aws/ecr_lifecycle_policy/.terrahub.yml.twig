  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_ecr_lifecycle_policy:
        {{ name }}:
          repository: '${var.{{ name }}_repository}'
          policy: '${var.{{ name }}_policy}'
    variable:
      {{ name }}_repository:
        type: 'string'
      {{ name }}_policy:
        type: 'string'
    output:
      id:
        value: '${aws_ecr_lifecycle_policy.{{ name }}.id}'
      thub_id:
        value: '${aws_ecr_lifecycle_policy.{{ name }}.id}'
      registry_id:
        value: '${aws_ecr_lifecycle_policy.{{ name }}.registry_id}'
