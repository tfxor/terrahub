  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_sagemaker_notebook_instance:
        {{ name }}:
          role_arn: var.{{ name }}_role_arn
          instance_type: var.{{ name }}_instance_type
          name: var.{{ name }}_name
          # subnet_id: ''
          # security_groups: []
          # kms_key_id: ''
          # lifecycle_config_name: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_role_arn:
        type: string
      {{ name }}_instance_type:
        type: string
      {{ name }}_name:
        type: string
    output:
      id:
        value: aws_sagemaker_notebook_instance.{{ name }}.id
      thub_id:
        value: aws_sagemaker_notebook_instance.{{ name }}.id
      security_groups:
        value: aws_sagemaker_notebook_instance.{{ name }}.security_groups
      arn:
        value: aws_sagemaker_notebook_instance.{{ name }}.arn
