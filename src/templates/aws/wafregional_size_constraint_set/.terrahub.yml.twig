  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_wafregional_size_constraint_set:
        {{ name }}:
    output:
      id:
        value: '${aws_wafregional_size_constraint_set.{{ name }}.id}'
      thub_id:
        value: '${aws_wafregional_size_constraint_set.{{ name }}.id}'
