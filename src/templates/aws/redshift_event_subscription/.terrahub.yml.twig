  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_redshift_event_subscription:
        {{ name }}:
          name: var.{{ name }}_name
          sns_topic_arn: var.{{ name }}_sns_topic_arn
          # source_ids: []
          # source_type: ''
          # severity: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
          # event_categories: []
          # enabled: false
    variable:
      {{ name }}_name:
        type: string
      {{ name }}_sns_topic_arn:
        type: string
    output:
      id:
        value: aws_redshift_event_subscription.{{ name }}.id
      thub_id:
        value: aws_redshift_event_subscription.{{ name }}.id
      arn:
        value: aws_redshift_event_subscription.{{ name }}.arn
      customer_aws_id:
        value: aws_redshift_event_subscription.{{ name }}.customer_aws_id
      status:
        value: aws_redshift_event_subscription.{{ name }}.status
