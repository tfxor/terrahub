  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_glue_job:
        {{ name }}:
          name: var.{{ name }}_name
          role_arn: var.{{ name }}_role_arn
          command: 
            -
              script_location: ''
          #     name: ''
          # allocated_capacity: 0
          # connections: []
          # default_arguments: 
          #   '[TO_BE_REPLACED]': ''
          # execution_property: 
          #   -
          #     max_concurrent_runs: 0
          # max_capacity: ''
          # security_configuration: ''
          # description: ''
          # max_retries: 0
          # timeout: 0
    variable:
      {{ name }}_name:
        type: string
      {{ name }}_role_arn:
        type: string
    output:
      id:
        value: aws_glue_job.{{ name }}.id
      thub_id:
        value: aws_glue_job.{{ name }}.id
      allocated_capacity:
        value: aws_glue_job.{{ name }}.allocated_capacity
      execution_property:
        value: aws_glue_job.{{ name }}.execution_property
      max_capacity:
        value: aws_glue_job.{{ name }}.max_capacity
