  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_cloudwatch_event_rule:
        {{ name }}:
          # event_pattern: ''
          # description: ''
          # is_enabled: false
          # schedule_expression: ''
          # name_prefix: ''
          # role_arn: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
          # name: ''
    output:
      id:
        value: aws_cloudwatch_event_rule.{{ name }}.id
      thub_id:
        value: aws_cloudwatch_event_rule.{{ name }}.id
      arn:
        value: aws_cloudwatch_event_rule.{{ name }}.arn
      name:
        value: aws_cloudwatch_event_rule.{{ name }}.name
