  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_globalaccelerator_accelerator:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          # enabled: false
          # attributes: 
          #   -
          #     flow_logs_s3_prefix: ''
          #     flow_logs_enabled: false
          #     flow_logs_s3_bucket: ''
          # ip_address_type: ''
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_globalaccelerator_accelerator.{{ name }}.id}'
      thub_id:
        value: '${aws_globalaccelerator_accelerator.{{ name }}.id}'
      ip_sets:
        value: '${aws_globalaccelerator_accelerator.{{ name }}.ip_sets}'
