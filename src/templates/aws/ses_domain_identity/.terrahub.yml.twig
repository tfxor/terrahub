  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_ses_domain_identity:
        {{ name }}:
          domain: var.{{ name }}_domain
    variable:
      {{ name }}_domain:
        type: string
    output:
      id:
        value: aws_ses_domain_identity.{{ name }}.id
      thub_id:
        value: aws_ses_domain_identity.{{ name }}.id
      arn:
        value: aws_ses_domain_identity.{{ name }}.arn
      verification_token:
        value: aws_ses_domain_identity.{{ name }}.verification_token
