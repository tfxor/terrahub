  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_iam_group_policy_attachment:
        {{ name }}:
          policy_arn: var.{{ name }}_policy_arn
          group: var.{{ name }}_group
    variable:
      {{ name }}_policy_arn:
        type: string
      {{ name }}_group:
        type: string
    output:
      id:
        value: aws_iam_group_policy_attachment.{{ name }}.id
      thub_id:
        value: aws_iam_group_policy_attachment.{{ name }}.id
