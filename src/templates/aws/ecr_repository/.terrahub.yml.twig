  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_ecr_repository:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          # tags: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_ecr_repository.{{ name }}.id}'
      thub_id:
        value: '${aws_ecr_repository.{{ name }}.id}'
      arn:
        value: '${aws_ecr_repository.{{ name }}.arn}'
      registry_id:
        value: '${aws_ecr_repository.{{ name }}.registry_id}'
      repository_url:
        value: '${aws_ecr_repository.{{ name }}.repository_url}'
