  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_docdb_cluster_instance:
        {{ name }}:
          instance_class: var.{{ name }}_instance_class
          cluster_identifier: var.{{ name }}_cluster_identifier
          # apply_immediately: false
          # preferred_maintenance_window: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
          # engine: ''
          # identifier_prefix: ''
          # promotion_tier: 0
          # auto_minor_version_upgrade: false
          # availability_zone: ''
          # identifier: ''
    variable:
      {{ name }}_instance_class:
        type: string
      {{ name }}_cluster_identifier:
        type: string
    output:
      id:
        value: aws_docdb_cluster_instance.{{ name }}.id
      thub_id:
        value: aws_docdb_cluster_instance.{{ name }}.id
      apply_immediately:
        value: aws_docdb_cluster_instance.{{ name }}.apply_immediately
      arn:
        value: aws_docdb_cluster_instance.{{ name }}.arn
      dbi_resource_id:
        value: aws_docdb_cluster_instance.{{ name }}.dbi_resource_id
      preferred_maintenance_window:
        value: aws_docdb_cluster_instance.{{ name }}.preferred_maintenance_window
      storage_encrypted:
        value: aws_docdb_cluster_instance.{{ name }}.storage_encrypted
      writer:
        value: aws_docdb_cluster_instance.{{ name }}.writer
      db_subnet_group_name:
        value: aws_docdb_cluster_instance.{{ name }}.db_subnet_group_name
      endpoint:
        value: aws_docdb_cluster_instance.{{ name }}.endpoint
      engine_version:
        value: aws_docdb_cluster_instance.{{ name }}.engine_version
      identifier_prefix:
        value: aws_docdb_cluster_instance.{{ name }}.identifier_prefix
      availability_zone:
        value: aws_docdb_cluster_instance.{{ name }}.availability_zone
      identifier:
        value: aws_docdb_cluster_instance.{{ name }}.identifier
      preferred_backup_window:
        value: aws_docdb_cluster_instance.{{ name }}.preferred_backup_window
      publicly_accessible:
        value: aws_docdb_cluster_instance.{{ name }}.publicly_accessible
      kms_key_id:
        value: aws_docdb_cluster_instance.{{ name }}.kms_key_id
      port:
        value: aws_docdb_cluster_instance.{{ name }}.port
