  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_appsync_graphql_api:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          authentication_type: '${var.{{ name }}_authentication_type}'
          # log_config: 
          #   -
          #     field_log_level: ''
          #     cloudwatch_logs_role_arn: ''
          # openid_connect_config: 
          #   -
          #     issuer: ''
          #     auth_ttl: 0
          #     client_id: ''
          #     iat_ttl: 0
          # user_pool_config: 
          #   -
          #     default_action: ''
          #     user_pool_id: ''
          #     app_id_client_regex: ''
          #     aws_region: ''
    variable:
      {{ name }}_name:
        type: 'string'
      {{ name }}_authentication_type:
        type: 'string'
    output:
      id:
        value: '${aws_appsync_graphql_api.{{ name }}.id}'
      thub_id:
        value: '${aws_appsync_graphql_api.{{ name }}.id}'
      arn:
        value: '${aws_appsync_graphql_api.{{ name }}.arn}'
      uris:
        value: '${aws_appsync_graphql_api.{{ name }}.uris}'
