  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_neptune_subnet_group:
        {{ name }}:
          subnet_ids: '${var.{{ name }}_subnet_ids}'
          # name: ''
          # name_prefix: ''
          # description: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_subnet_ids:
        type: 'set'
    output:
      id:
        value: '${aws_neptune_subnet_group.{{ name }}.id}'
      thub_id:
        value: '${aws_neptune_subnet_group.{{ name }}.id}'
      name:
        value: '${aws_neptune_subnet_group.{{ name }}.name}'
      name_prefix:
        value: '${aws_neptune_subnet_group.{{ name }}.name_prefix}'
      arn:
        value: '${aws_neptune_subnet_group.{{ name }}.arn}'
