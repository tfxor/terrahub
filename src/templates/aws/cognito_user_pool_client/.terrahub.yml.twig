  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_cognito_user_pool_client:
        {{ name }}:
          user_pool_id: '${var.{{ name }}_user_pool_id}'
          name: '${var.{{ name }}_name}'
          # default_redirect_uri: ''
          # generate_secret: false
          # read_attributes: []
          # write_attributes: []
          # allowed_oauth_scopes: []
          # explicit_auth_flows: []
          # allowed_oauth_flows: []
          # supported_identity_providers: []
          # allowed_oauth_flows_user_pool_client: false
          # callback_urls: []
          # logout_urls: []
          # refresh_token_validity: 0
    variable:
      {{ name }}_user_pool_id:
        type: 'string'
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_cognito_user_pool_client.{{ name }}.id}'
      thub_id:
        value: '${aws_cognito_user_pool_client.{{ name }}.id}'
      client_secret:
        value: '${aws_cognito_user_pool_client.{{ name }}.client_secret}'
