  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_api_gateway_gateway_response:
        {{ name }}:
          rest_api_id: '${var.{{ name }}_rest_api_id}'
          response_type: '${var.{{ name }}_response_type}'
          # status_code: ''
          # response_templates: 
          #   '[TO_BE_REPLACED]': ''
          # response_parameters: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_rest_api_id:
        type: 'string'
      {{ name }}_response_type:
        type: 'string'
    output:
      id:
        value: '${aws_api_gateway_gateway_response.{{ name }}.id}'
      thub_id:
        value: '${aws_api_gateway_gateway_response.{{ name }}.id}'
