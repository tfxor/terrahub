  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_datasync_location_nfs:
        {{ name }}:
          subdirectory: var.{{ name }}_subdirectory
          on_prem_config: 
            -
              agent_arns: []
          server_hostname: var.{{ name }}_server_hostname
          # tags: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_subdirectory:
        type: string
      {{ name }}_server_hostname:
        type: string
    output:
      id:
        value: aws_datasync_location_nfs.{{ name }}.id
      thub_id:
        value: aws_datasync_location_nfs.{{ name }}.id
      uri:
        value: aws_datasync_location_nfs.{{ name }}.uri
      arn:
        value: aws_datasync_location_nfs.{{ name }}.arn
