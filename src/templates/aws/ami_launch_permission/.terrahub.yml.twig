  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_ami_launch_permission:
        {{ name }}:
          image_id: var.{{ name }}_image_id
          account_id: var.{{ name }}_account_id
    variable:
      {{ name }}_image_id:
        type: string
      {{ name }}_account_id:
        type: string
    output:
      id:
        value: aws_ami_launch_permission.{{ name }}.id
      thub_id:
        value: aws_ami_launch_permission.{{ name }}.id
