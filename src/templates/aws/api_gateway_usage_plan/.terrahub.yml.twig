  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_api_gateway_usage_plan:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          # description: ''
          # api_stages:
          #   -
          #     stage: ''
          #     api_id: ''
          # quota_settings:
          #   period: ''
          #   limit: 0
          # throttle_settings:
          # product_code: ''
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_api_gateway_usage_plan.{{ name }}.id}'
      thub_id:
        value: '${aws_api_gateway_usage_plan.{{ name }}.id}'
