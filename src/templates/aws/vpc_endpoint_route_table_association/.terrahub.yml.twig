  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_vpc_endpoint_route_table_association:
        {{ name }}:
          vpc_endpoint_id: '${var.{{ name }}_vpc_endpoint_id}'
          route_table_id: '${var.{{ name }}_route_table_id}'
    variable:
      {{ name }}_vpc_endpoint_id:
        type: 'string'
      {{ name }}_route_table_id:
        type: 'string'
    output:
      id:
        value: '${aws_vpc_endpoint_route_table_association.{{ name }}.id}'
      thub_id:
        value: '${aws_vpc_endpoint_route_table_association.{{ name }}.id}'
