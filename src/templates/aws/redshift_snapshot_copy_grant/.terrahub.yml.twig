  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_redshift_snapshot_copy_grant:
        {{ name }}:
          snapshot_copy_grant_name: '${var.{{ name }}_snapshot_copy_grant_name}'
          # kms_key_id: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_snapshot_copy_grant_name:
        type: 'string'
    output:
      id:
        value: '${aws_redshift_snapshot_copy_grant.{{ name }}.id}'
      thub_id:
        value: '${aws_redshift_snapshot_copy_grant.{{ name }}.id}'
      kms_key_id:
        value: '${aws_redshift_snapshot_copy_grant.{{ name }}.kms_key_id}'
