# aws_cognito_identity_pool_roles_attachment

Provides an AWS Cognito Identity Pool Roles Attachment.

## input variables

| Name | Description | Type | Default | Required |
|------|-------------|:----:|:-----:|:-----:|
|identity_pool_id|An identity pool ID in the format REGION:GUID.|string|{{ name }}|Yes|
|role_mapping|A List of Role Mapping.|list||No|
|roles|The map of roles associated with this pool. For a given role, the key will be either "authenticated" or "unauthenticated" and the value will be the Role ARN.|map||Yes|
 
## output parameters

| Name | Description | Type |
|------|-------------|:----:|
|id|The identity pool ID.|string|
|identity_pool_id|An identity pool ID in the format REGION:GUID.|string|
|role_mapping|The List of Role Mapping.|list|
|roles|The map of roles associated with this pool. For a given role, the key will be either "authenticated" or "unauthenticated" and the value will be the Role ARN.|map|
