  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_api_gateway_stage:
        {{ name }}:
          deployment_id: '${var.{{ name }}_deployment_id}'
          rest_api_id: '${var.{{ name }}_rest_api_id}'
          stage_name: '${var.{{ name }}_stage_name}'
          # description: ''
          # xray_tracing_enabled: false
          # client_certificate_id: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
          # cache_cluster_enabled: false
          # cache_cluster_size: ''
          # documentation_version: ''
          # variables: 
          #   '[TO_BE_REPLACED]': ''
          # access_log_settings: 
          #   -
          #     destination_arn: ''
          #     format: ''
    variable:
      {{ name }}_deployment_id:
        type: 'string'
      {{ name }}_rest_api_id:
        type: 'string'
      {{ name }}_stage_name:
        type: 'string'
    output:
      id:
        value: '${aws_api_gateway_stage.{{ name }}.id}'
      thub_id:
        value: '${aws_api_gateway_stage.{{ name }}.id}'
      execution_arn:
        value: '${aws_api_gateway_stage.{{ name }}.execution_arn}'
      invoke_url:
        value: '${aws_api_gateway_stage.{{ name }}.invoke_url}'
