  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_guardduty_detector:
        {{ name }}:
          # enable: false
          # finding_publishing_frequency: ''
    output:
      id:
        value: aws_guardduty_detector.{{ name }}.id
      thub_id:
        value: aws_guardduty_detector.{{ name }}.id
      account_id:
        value: aws_guardduty_detector.{{ name }}.account_id
      finding_publishing_frequency:
        value: aws_guardduty_detector.{{ name }}.finding_publishing_frequency
