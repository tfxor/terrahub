  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_guardduty_detector:
        {{ name }}:
          # enable: false
          # finding_publishing_frequency: ''
    output:
      id:
        value: '${aws_guardduty_detector.{{ name }}.id}'
      thub_id:
        value: '${aws_guardduty_detector.{{ name }}.id}'
      account_id:
        value: '${aws_guardduty_detector.{{ name }}.account_id}'
