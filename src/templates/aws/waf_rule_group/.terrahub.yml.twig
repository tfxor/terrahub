  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_waf_rule_group:
        {{ name }}:
          metric_name: var.{{ name }}_metric_name
          name: var.{{ name }}_name
          # activated_rule: 
          #   action: 
          #     -
          #       type: ''
    variable:
      {{ name }}_metric_name:
        type: string
      {{ name }}_name:
        type: string
    output:
      id:
        value: aws_waf_rule_group.{{ name }}.id
      thub_id:
        value: aws_waf_rule_group.{{ name }}.id
