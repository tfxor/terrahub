  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_internet_gateway:
        {{ name }}:
          # vpc_id: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
    output:
      id:
        value: aws_internet_gateway.{{ name }}.id
      thub_id:
        value: aws_internet_gateway.{{ name }}.id
      owner_id:
        value: aws_internet_gateway.{{ name }}.owner_id
