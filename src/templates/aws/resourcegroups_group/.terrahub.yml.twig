  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_resourcegroups_group:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          resource_query: 
            -
              query: ''
          #     type: ''
          # description: ''
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_resourcegroups_group.{{ name }}.id}'
      thub_id:
        value: '${aws_resourcegroups_group.{{ name }}.id}'
      arn:
        value: '${aws_resourcegroups_group.{{ name }}.arn}'
