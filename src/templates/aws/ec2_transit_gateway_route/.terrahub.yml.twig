  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_ec2_transit_gateway_route:
        {{ name }}:
          destination_cidr_block: '${var.{{ name }}_destination_cidr_block}'
          transit_gateway_attachment_id: '${var.{{ name }}_transit_gateway_attachment_id}'
          transit_gateway_route_table_id: '${var.{{ name }}_transit_gateway_route_table_id}'
    variable:
      {{ name }}_destination_cidr_block:
        type: 'string'
      {{ name }}_transit_gateway_attachment_id:
        type: 'string'
      {{ name }}_transit_gateway_route_table_id:
        type: 'string'
    output:
      id:
        value: '${aws_ec2_transit_gateway_route.{{ name }}.id}'
      thub_id:
        value: '${aws_ec2_transit_gateway_route.{{ name }}.id}'
