  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_sns_topic_subscription:
        {{ name }}:
          endpoint: '${var.{{ name }}_endpoint}'
          topic_arn: '${var.{{ name }}_topic_arn}'
          protocol: '${var.{{ name }}_protocol}'
          # endpoint_auto_confirms: false
          # filter_policy: ''
          # delivery_policy: ''
          # raw_message_delivery: false
          # confirmation_timeout_in_minutes: 0
    variable:
      {{ name }}_endpoint:
        type: 'string'
      {{ name }}_topic_arn:
        type: 'string'
      {{ name }}_protocol:
        type: 'string'
    output:
      id:
        value: '${aws_sns_topic_subscription.{{ name }}.id}'
      thub_id:
        value: '${aws_sns_topic_subscription.{{ name }}.id}'
      arn:
        value: '${aws_sns_topic_subscription.{{ name }}.arn}'
