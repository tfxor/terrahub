  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_cloudtrail:
        {{ name }}:
          s3_bucket_name: var.{{ name }}_s3_bucket_name
          name: var.{{ name }}_name
          # event_selector: 
          #   -
          #     read_write_type: ''
          #     include_management_events: false
          #     data_resource: 
          #       -
          #         type: ''
          #         values: []
          # include_global_service_events: false
          # is_multi_region_trail: false
          # is_organization_trail: false
          # enable_logging: false
          # cloud_watch_logs_role_arn: ''
          # enable_log_file_validation: false
          # tags: 
          #   '[TO_BE_REPLACED]': ''
          # sns_topic_name: ''
          # kms_key_id: ''
          # s3_key_prefix: ''
          # cloud_watch_logs_group_arn: ''
    variable:
      {{ name }}_s3_bucket_name:
        type: string
      {{ name }}_name:
        type: string
    output:
      id:
        value: aws_cloudtrail.{{ name }}.id
      thub_id:
        value: aws_cloudtrail.{{ name }}.id
      home_region:
        value: aws_cloudtrail.{{ name }}.home_region
      arn:
        value: aws_cloudtrail.{{ name }}.arn
