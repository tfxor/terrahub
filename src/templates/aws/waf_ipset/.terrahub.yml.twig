  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_waf_ipset:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          # ip_set_descriptors:
          #   type: ''
          #   value: ''
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_waf_ipset.{{ name }}.id}'
      thub_id:
        value: '${aws_waf_ipset.{{ name }}.id}'
      arn:
        value: '${aws_waf_ipset.{{ name }}.arn}'
