  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_lb_listener_certificate:
        {{ name }}:
          certificate_arn: var.{{ name }}_certificate_arn
          listener_arn: var.{{ name }}_listener_arn
    variable:
      {{ name }}_certificate_arn:
        type: string
      {{ name }}_listener_arn:
        type: string
    output:
      id:
        value: aws_lb_listener_certificate.{{ name }}.id
      thub_id:
        value: aws_lb_listener_certificate.{{ name }}.id
