  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_sns_platform_application:
        {{ name }}:
          platform_credential: var.{{ name }}_platform_credential
          platform: var.{{ name }}_platform
          name: var.{{ name }}_name
          # event_endpoint_created_topic_arn: ''
          # event_endpoint_deleted_topic_arn: ''
          # platform_principal: ''
          # event_delivery_failure_topic_arn: ''
          # event_endpoint_updated_topic_arn: ''
          # failure_feedback_role_arn: ''
          # success_feedback_role_arn: ''
          # success_feedback_sample_rate: ''
    variable:
      {{ name }}_platform_credential:
        type: string
      {{ name }}_platform:
        type: string
      {{ name }}_name:
        type: string
    output:
      id:
        value: aws_sns_platform_application.{{ name }}.id
      thub_id:
        value: aws_sns_platform_application.{{ name }}.id
      arn:
        value: aws_sns_platform_application.{{ name }}.arn
