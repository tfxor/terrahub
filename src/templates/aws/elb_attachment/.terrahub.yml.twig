  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_elb_attachment:
        {{ name }}:
          elb: '${var.{{ name }}_elb}'
          instance: '${var.{{ name }}_instance}'
    variable:
      {{ name }}_elb:
        type: 'string'
      {{ name }}_instance:
        type: 'string'
    output:
      id:
        value: '${aws_elb_attachment.{{ name }}.id}'
      thub_id:
        value: '${aws_elb_attachment.{{ name }}.id}'
