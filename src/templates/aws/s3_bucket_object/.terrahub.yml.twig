  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_s3_bucket_object:
        {{ name }}:
          key: '${var.{{ name }}_key}'
          bucket: '${var.{{ name }}_bucket}'
          # cache_control: ''
          # content_encoding: ''
          # source: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
          # website_redirect: ''
          # content_type: ''
          # content: ''
          # content_base64: ''
          # storage_class: ''
          # server_side_encryption: ''
          # kms_key_id: ''
          # etag: ''
          # content_disposition: ''
          # content_language: ''
    variable:
      {{ name }}_key:
        type: 'string'
      {{ name }}_bucket:
        type: 'string'
    output:
      id:
        value: '${aws_s3_bucket_object.{{ name }}.id}'
      thub_id:
        value: '${aws_s3_bucket_object.{{ name }}.id}'
      content_type:
        value: '${aws_s3_bucket_object.{{ name }}.content_type}'
      storage_class:
        value: '${aws_s3_bucket_object.{{ name }}.storage_class}'
      server_side_encryption:
        value: '${aws_s3_bucket_object.{{ name }}.server_side_encryption}'
      etag:
        value: '${aws_s3_bucket_object.{{ name }}.etag}'
      version_id:
        value: '${aws_s3_bucket_object.{{ name }}.version_id}'
