  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_codepipeline_webhook:
        {{ name }}:
          authentication: '${var.{{ name }}_authentication}'
          filter:
            json_path: ''
            match_equals: ''
          name: '${var.{{ name }}_name}'
          target_action: '${var.{{ name }}_target_action}'
          target_pipeline: '${var.{{ name }}_target_pipeline}'
          # authentication_configuration:
          #   -
          #     secret_token: ''
          #     allowed_ip_range: ''
    variable:
      {{ name }}_authentication:
        type: 'string'
      {{ name }}_name:
        type: 'string'
      {{ name }}_target_action:
        type: 'string'
      {{ name }}_target_pipeline:
        type: 'string'
    output:
      id:
        value: '${aws_codepipeline_webhook.{{ name }}.id}'
      thub_id:
        value: '${aws_codepipeline_webhook.{{ name }}.id}'
      url:
        value: '${aws_codepipeline_webhook.{{ name }}.url}'
