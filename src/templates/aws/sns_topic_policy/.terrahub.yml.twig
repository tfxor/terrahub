  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_sns_topic_policy:
        {{ name }}:
          arn: '${var.{{ name }}_arn}'
          policy: '${var.{{ name }}_policy}'
    variable:
      {{ name }}_arn:
        type: 'string'
      {{ name }}_policy:
        type: 'string'
    output:
      id:
        value: '${aws_sns_topic_policy.{{ name }}.id}'
      thub_id:
        value: '${aws_sns_topic_policy.{{ name }}.id}'
