  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_waf_size_constraint_set:
        {{ name }}:
    output:
      id:
        value: aws_waf_size_constraint_set.{{ name }}.id
      thub_id:
        value: aws_waf_size_constraint_set.{{ name }}.id
