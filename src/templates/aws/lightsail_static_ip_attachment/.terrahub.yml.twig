  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_lightsail_static_ip_attachment:
        {{ name }}:
          instance_name: '${var.{{ name }}_instance_name}'
          static_ip_name: '${var.{{ name }}_static_ip_name}'
    variable:
      {{ name }}_instance_name:
        type: 'string'
      {{ name }}_static_ip_name:
        type: 'string'
    output:
      id:
        value: '${aws_lightsail_static_ip_attachment.{{ name }}.id}'
      thub_id:
        value: '${aws_lightsail_static_ip_attachment.{{ name }}.id}'
