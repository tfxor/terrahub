  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_iot_certificate:
        {{ name }}:
          csr: '${var.{{ name }}_csr}'
          active: '${var.{{ name }}_active}'
    variable:
      {{ name }}_csr:
        type: 'string'
      {{ name }}_active:
        type: 'bool'
    output:
      id:
        value: '${aws_iot_certificate.{{ name }}.id}'
      thub_id:
        value: '${aws_iot_certificate.{{ name }}.id}'
      arn:
        value: '${aws_iot_certificate.{{ name }}.arn}'
