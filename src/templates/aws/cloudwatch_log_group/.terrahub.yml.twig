  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_cloudwatch_log_group:
        {{ name }}:
          # retention_in_days: 0
          # kms_key_id: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
          # name: ''
          # name_prefix: ''
    output:
      id:
        value: '${aws_cloudwatch_log_group.{{ name }}.id}'
      thub_id:
        value: '${aws_cloudwatch_log_group.{{ name }}.id}'
      arn:
        value: '${aws_cloudwatch_log_group.{{ name }}.arn}'
      name:
        value: '${aws_cloudwatch_log_group.{{ name }}.name}'
