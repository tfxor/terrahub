  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_db_instance_role_association:
        {{ name }}:
          db_instance_identifier: var.{{ name }}_db_instance_identifier
          feature_name: var.{{ name }}_feature_name
          role_arn: var.{{ name }}_role_arn
    variable:
      {{ name }}_db_instance_identifier:
        type: string
      {{ name }}_feature_name:
        type: string
      {{ name }}_role_arn:
        type: string
    output:
      id:
        value: aws_db_instance_role_association.{{ name }}.id
      thub_id:
        value: aws_db_instance_role_association.{{ name }}.id
