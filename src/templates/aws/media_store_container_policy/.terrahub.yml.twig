  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_media_store_container_policy:
        {{ name }}:
          policy: '${var.{{ name }}_policy}'
          container_name: '${var.{{ name }}_container_name}'
    variable:
      {{ name }}_policy:
        type: 'string'
      {{ name }}_container_name:
        type: 'string'
    output:
      id:
        value: '${aws_media_store_container_policy.{{ name }}.id}'
      thub_id:
        value: '${aws_media_store_container_policy.{{ name }}.id}'
