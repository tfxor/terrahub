  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_ses_template:
        {{ name }}:
          name: var.{{ name }}_name
          # html: ''
          # subject: ''
          # text: ''
    variable:
      {{ name }}_name:
        type: string
    output:
      id:
        value: aws_ses_template.{{ name }}.id
      thub_id:
        value: aws_ses_template.{{ name }}.id
