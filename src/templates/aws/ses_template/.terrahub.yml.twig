  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_ses_template:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          # subject: ''
          # text: ''
          # html: ''
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_ses_template.{{ name }}.id}'
      thub_id:
        value: '${aws_ses_template.{{ name }}.id}'
