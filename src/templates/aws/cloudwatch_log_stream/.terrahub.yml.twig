  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_cloudwatch_log_stream:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          log_group_name: '${var.{{ name }}_log_group_name}'
    variable:
      {{ name }}_name:
        type: 'string'
      {{ name }}_log_group_name:
        type: 'string'
    output:
      id:
        value: '${aws_cloudwatch_log_stream.{{ name }}.id}'
      thub_id:
        value: '${aws_cloudwatch_log_stream.{{ name }}.id}'
      arn:
        value: '${aws_cloudwatch_log_stream.{{ name }}.arn}'
