  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_dms_replication_instance:
        {{ name }}:
          replication_instance_id: '${var.{{ name }}_replication_instance_id}'
          replication_instance_class: '${var.{{ name }}_replication_instance_class}'
          # engine_version: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
          # vpc_security_group_ids: []
          # apply_immediately: false
          # auto_minor_version_upgrade: false
          # kms_key_arn: ''
          # preferred_maintenance_window: ''
          # allocated_storage: 0
          # availability_zone: ''
          # multi_az: false
          # publicly_accessible: false
          # replication_subnet_group_id: ''
    variable:
      {{ name }}_replication_instance_id:
        type: 'string'
      {{ name }}_replication_instance_class:
        type: 'string'
    output:
      id:
        value: '${aws_dms_replication_instance.{{ name }}.id}'
      thub_id:
        value: '${aws_dms_replication_instance.{{ name }}.id}'
      engine_version:
        value: '${aws_dms_replication_instance.{{ name }}.engine_version}'
      replication_instance_arn:
        value: '${aws_dms_replication_instance.{{ name }}.replication_instance_arn}'
      vpc_security_group_ids:
        value: '${aws_dms_replication_instance.{{ name }}.vpc_security_group_ids}'
      auto_minor_version_upgrade:
        value: '${aws_dms_replication_instance.{{ name }}.auto_minor_version_upgrade}'
      kms_key_arn:
        value: '${aws_dms_replication_instance.{{ name }}.kms_key_arn}'
      preferred_maintenance_window:
        value: '${aws_dms_replication_instance.{{ name }}.preferred_maintenance_window}'
      allocated_storage:
        value: '${aws_dms_replication_instance.{{ name }}.allocated_storage}'
      availability_zone:
        value: '${aws_dms_replication_instance.{{ name }}.availability_zone}'
      multi_az:
        value: '${aws_dms_replication_instance.{{ name }}.multi_az}'
      replication_instance_public_ips:
        value: '${aws_dms_replication_instance.{{ name }}.replication_instance_public_ips}'
      publicly_accessible:
        value: '${aws_dms_replication_instance.{{ name }}.publicly_accessible}'
      replication_instance_private_ips:
        value: '${aws_dms_replication_instance.{{ name }}.replication_instance_private_ips}'
      replication_subnet_group_id:
        value: '${aws_dms_replication_instance.{{ name }}.replication_subnet_group_id}'
