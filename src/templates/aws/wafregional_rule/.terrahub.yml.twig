  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_wafregional_rule:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          metric_name: '${var.{{ name }}_metric_name}'
          # predicate: 
          #   negated: false
          #   data_id: ''
          #   type: ''
    variable:
      {{ name }}_name:
        type: 'string'
      {{ name }}_metric_name:
        type: 'string'
    output:
      id:
        value: '${aws_wafregional_rule.{{ name }}.id}'
      thub_id:
        value: '${aws_wafregional_rule.{{ name }}.id}'
