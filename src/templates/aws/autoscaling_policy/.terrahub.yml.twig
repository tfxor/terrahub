  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_autoscaling_policy:
        {{ name }}:
          target_value: var.{{ name }}_target_value
          name: var.{{ name }}_name
          autoscaling_group_name: var.{{ name }}_autoscaling_group_name
          # disable_scale_in: false
          # estimated_instance_warmup: 0
          # min_adjustment_magnitude: 0
          # min_adjustment_step: 0
          # step_adjustment: 
          #   scaling_adjustment: 0
          #   metric_interval_lower_bound: ''
          #   metric_interval_upper_bound: ''
          # scaling_adjustment: 0
          # target_tracking_configuration: 
          #   -
          #     predefined_metric_specification: 
          #       -
          #         predefined_metric_type: ''
          #         resource_label: ''
          # adjustment_type: ''
          # policy_type: ''
          # cooldown: 0
          # metric_aggregation_type: ''
    variable:
      {{ name }}_target_value:
        type: float
      {{ name }}_name:
        type: string
      {{ name }}_autoscaling_group_name:
        type: string
    output:
      id:
        value: aws_autoscaling_policy.{{ name }}.id
      thub_id:
        value: aws_autoscaling_policy.{{ name }}.id
      arn:
        value: aws_autoscaling_policy.{{ name }}.arn
      metric_aggregation_type:
        value: aws_autoscaling_policy.{{ name }}.metric_aggregation_type
