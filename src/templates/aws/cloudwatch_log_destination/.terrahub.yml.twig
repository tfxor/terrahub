  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_cloudwatch_log_destination:
        {{ name }}:
          target_arn: var.{{ name }}_target_arn
          name: var.{{ name }}_name
          role_arn: var.{{ name }}_role_arn
    variable:
      {{ name }}_target_arn:
        type: string
      {{ name }}_name:
        type: string
      {{ name }}_role_arn:
        type: string
    output:
      id:
        value: aws_cloudwatch_log_destination.{{ name }}.id
      thub_id:
        value: aws_cloudwatch_log_destination.{{ name }}.id
      arn:
        value: aws_cloudwatch_log_destination.{{ name }}.arn
