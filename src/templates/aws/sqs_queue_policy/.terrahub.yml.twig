  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_sqs_queue_policy:
        {{ name }}:
          policy: var.{{ name }}_policy
          queue_url: var.{{ name }}_queue_url
    variable:
      {{ name }}_policy:
        type: string
      {{ name }}_queue_url:
        type: string
    output:
      id:
        value: aws_sqs_queue_policy.{{ name }}.id
      thub_id:
        value: aws_sqs_queue_policy.{{ name }}.id
