  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_pinpoint_adm_channel:
        {{ name }}:
          application_id: var.{{ name }}_application_id
          client_id: var.{{ name }}_client_id
          client_secret: var.{{ name }}_client_secret
          # enabled: false
    variable:
      {{ name }}_application_id:
        type: string
      {{ name }}_client_id:
        type: string
      {{ name }}_client_secret:
        type: string
    output:
      id:
        value: aws_pinpoint_adm_channel.{{ name }}.id
      thub_id:
        value: aws_pinpoint_adm_channel.{{ name }}.id
