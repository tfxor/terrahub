  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_iam_access_key:
        {{ name }}:
          user: '${var.{{ name }}_user}'
          # pgp_key: ''
    variable:
      {{ name }}_user:
        type: 'string'
    output:
      id:
        value: '${aws_iam_access_key.{{ name }}.id}'
      thub_id:
        value: '${aws_iam_access_key.{{ name }}.id}'
      key_fingerprint:
        value: '${aws_iam_access_key.{{ name }}.key_fingerprint}'
      encrypted_secret:
        value: '${aws_iam_access_key.{{ name }}.encrypted_secret}'
      status:
        value: '${aws_iam_access_key.{{ name }}.status}'
      secret:
        value: '${aws_iam_access_key.{{ name }}.secret}'
      ses_smtp_password:
        value: '${aws_iam_access_key.{{ name }}.ses_smtp_password}'
