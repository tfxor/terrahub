  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_iam_user_policy:
        {{ name }}:
          user: '${var.{{ name }}_user}'
          policy: '${var.{{ name }}_policy}'
          # name_prefix: ''
          # name: ''
    variable:
      {{ name }}_user:
        type: 'string'
      {{ name }}_policy:
        type: 'string'
    output:
      id:
        value: '${aws_iam_user_policy.{{ name }}.id}'
      thub_id:
        value: '${aws_iam_user_policy.{{ name }}.id}'
      name:
        value: '${aws_iam_user_policy.{{ name }}.name}'
