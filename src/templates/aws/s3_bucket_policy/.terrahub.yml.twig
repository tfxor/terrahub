  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_s3_bucket_policy:
        {{ name }}:
          bucket: '${var.{{ name }}_bucket}'
          policy: '${var.{{ name }}_policy}'
    variable:
      {{ name }}_bucket:
        type: 'string'
      {{ name }}_policy:
        type: 'string'
    output:
      id:
        value: '${aws_s3_bucket_policy.{{ name }}.id}'
      thub_id:
        value: '${aws_s3_bucket_policy.{{ name }}.id}'
