  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_athena_workgroup:
        {{ name }}:
          name: var.{{ name }}_name
          # configuration: []
          # description: ''
          # state: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_name:
        type: string
    output:
      id:
        value: aws_athena_workgroup.{{ name }}.id
      thub_id:
        value: aws_athena_workgroup.{{ name }}.id
      arn:
        value: aws_athena_workgroup.{{ name }}.arn
