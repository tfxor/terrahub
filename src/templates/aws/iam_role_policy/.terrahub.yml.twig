  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_iam_role_policy:
        {{ name }}:
          role: '${var.{{ name }}_role}'
          policy: '${var.{{ name }}_policy}'
          # name: ''
          # name_prefix: ''
    variable:
      {{ name }}_role:
        type: 'string'
      {{ name }}_policy:
        type: 'string'
    output:
      id:
        value: '${aws_iam_role_policy.{{ name }}.id}'
      thub_id:
        value: '${aws_iam_role_policy.{{ name }}.id}'
      name:
        value: '${aws_iam_role_policy.{{ name }}.name}'
