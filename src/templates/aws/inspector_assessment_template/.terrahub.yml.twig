  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_inspector_assessment_template:
        {{ name }}:
          rules_package_arns: var.{{ name }}_rules_package_arns
          name: var.{{ name }}_name
          target_arn: var.{{ name }}_target_arn
          duration: var.{{ name }}_duration
    variable:
      {{ name }}_rules_package_arns:
        type: set
      {{ name }}_name:
        type: string
      {{ name }}_target_arn:
        type: string
      {{ name }}_duration:
        type: int
    output:
      id:
        value: aws_inspector_assessment_template.{{ name }}.id
      thub_id:
        value: aws_inspector_assessment_template.{{ name }}.id
      arn:
        value: aws_inspector_assessment_template.{{ name }}.arn
