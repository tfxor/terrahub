  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_redshift_parameter_group:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          family: '${var.{{ name }}_family}'
          # description: ''
          # parameter: 
          #   name: ''
          #   value: ''
    variable:
      {{ name }}_name:
        type: 'string'
      {{ name }}_family:
        type: 'string'
    output:
      id:
        value: '${aws_redshift_parameter_group.{{ name }}.id}'
      thub_id:
        value: '${aws_redshift_parameter_group.{{ name }}.id}'
