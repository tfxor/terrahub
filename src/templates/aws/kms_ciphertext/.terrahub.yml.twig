  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_kms_ciphertext:
        {{ name }}:
          plaintext: var.{{ name }}_plaintext
          key_id: var.{{ name }}_key_id
          # context: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_plaintext:
        type: string
      {{ name }}_key_id:
        type: string
    output:
      id:
        value: aws_kms_ciphertext.{{ name }}.id
      thub_id:
        value: aws_kms_ciphertext.{{ name }}.id
      ciphertext_blob:
        value: aws_kms_ciphertext.{{ name }}.ciphertext_blob
