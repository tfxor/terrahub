  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_glue_catalog_database:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          # catalog_id: ''
          # description: ''
          # location_uri: ''
          # parameters: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_glue_catalog_database.{{ name }}.id}'
      thub_id:
        value: '${aws_glue_catalog_database.{{ name }}.id}'
      catalog_id:
        value: '${aws_glue_catalog_database.{{ name }}.catalog_id}'
