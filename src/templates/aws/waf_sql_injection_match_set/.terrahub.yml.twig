  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_waf_sql_injection_match_set:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          # sql_injection_match_tuples: 
          #   field_to_match: 
          #     type: ''
          #     data: ''
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_waf_sql_injection_match_set.{{ name }}.id}'
      thub_id:
        value: '${aws_waf_sql_injection_match_set.{{ name }}.id}'
