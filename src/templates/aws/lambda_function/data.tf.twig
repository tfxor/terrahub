# data "external" "{{ name }}" {
#   program = ["node", "./scripts/build.js"]
#   query = {
#     component = "{{ name }}"
#   }
# }

# data "null_data_source" "{{ name }}" {
#   depends_on = ["data.external.{{ name }}"]
#   inputs = {
#     output_path = "${path.module}/${var.lambda_runtime}.zip"
#   }
# }
