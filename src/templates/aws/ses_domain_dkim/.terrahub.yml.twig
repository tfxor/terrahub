  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_ses_domain_dkim:
        {{ name }}:
          domain: '${var.{{ name }}_domain}'
    variable:
      {{ name }}_domain:
        type: 'string'
    output:
      id:
        value: '${aws_ses_domain_dkim.{{ name }}.id}'
      thub_id:
        value: '${aws_ses_domain_dkim.{{ name }}.id}'
      dkim_tokens:
        value: '${aws_ses_domain_dkim.{{ name }}.dkim_tokens}'
