  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_iot_policy_attachment:
        {{ name }}:
          target: '${var.{{ name }}_target}'
          policy: '${var.{{ name }}_policy}'
    variable:
      {{ name }}_target:
        type: 'string'
      {{ name }}_policy:
        type: 'string'
    output:
      id:
        value: '${aws_iot_policy_attachment.{{ name }}.id}'
      thub_id:
        value: '${aws_iot_policy_attachment.{{ name }}.id}'
