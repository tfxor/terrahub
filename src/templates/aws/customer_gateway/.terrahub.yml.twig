  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_customer_gateway:
        {{ name }}:
          bgp_asn: var.{{ name }}_bgp_asn
          ip_address: var.{{ name }}_ip_address
          type: var.{{ name }}_type
          # tags: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_bgp_asn:
        type: int
      {{ name }}_ip_address:
        type: string
      {{ name }}_type:
        type: string
    output:
      id:
        value: aws_customer_gateway.{{ name }}.id
      thub_id:
        value: aws_customer_gateway.{{ name }}.id
