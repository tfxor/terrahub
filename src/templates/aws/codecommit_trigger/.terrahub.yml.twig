  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_codecommit_trigger:
        {{ name }}:
          repository_name: '${var.{{ name }}_repository_name}'
          trigger:
            events: []
            name: ''
            destination_arn: ''
          #   branches:
          #     -
          #   custom_data: ''
    variable:
      {{ name }}_repository_name:
        type: 'string'
    output:
      id:
        value: '${aws_codecommit_trigger.{{ name }}.id}'
      thub_id:
        value: '${aws_codecommit_trigger.{{ name }}.id}'
      configuration_id:
        value: '${aws_codecommit_trigger.{{ name }}.configuration_id}'
