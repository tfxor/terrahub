  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_ssm_activation:
        {{ name }}:
          iam_role: '${var.{{ name }}_iam_role}'
          # description: ''
          # expiration_date: ''
          # registration_limit: 0
          # name: ''
    variable:
      {{ name }}_iam_role:
        type: 'string'
    output:
      id:
        value: '${aws_ssm_activation.{{ name }}.id}'
      thub_id:
        value: '${aws_ssm_activation.{{ name }}.id}'
      expired:
        value: '${aws_ssm_activation.{{ name }}.expired}'
      registration_count:
        value: '${aws_ssm_activation.{{ name }}.registration_count}'
      activation_code:
        value: '${aws_ssm_activation.{{ name }}.activation_code}'
