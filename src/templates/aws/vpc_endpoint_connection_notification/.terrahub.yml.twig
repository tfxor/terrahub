  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_vpc_endpoint_connection_notification:
        {{ name }}:
          connection_notification_arn: var.{{ name }}_connection_notification_arn
          connection_events: var.{{ name }}_connection_events
          # vpc_endpoint_service_id: ''
          # vpc_endpoint_id: ''
    variable:
      {{ name }}_connection_notification_arn:
        type: string
      {{ name }}_connection_events:
        type: set
    output:
      id:
        value: aws_vpc_endpoint_connection_notification.{{ name }}.id
      thub_id:
        value: aws_vpc_endpoint_connection_notification.{{ name }}.id
      state:
        value: aws_vpc_endpoint_connection_notification.{{ name }}.state
      notification_type:
        value: aws_vpc_endpoint_connection_notification.{{ name }}.notification_type
