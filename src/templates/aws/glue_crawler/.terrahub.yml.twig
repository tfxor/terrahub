  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_glue_crawler:
        {{ name }}:
          name: var.{{ name }}_name
          role: var.{{ name }}_role
          database_name: var.{{ name }}_database_name
          # schema_change_policy: 
          #   -
          #     delete_behavior: ''
          #     update_behavior: ''
          # dynamodb_target: 
          #   -
          #     path: ''
          # security_configuration: ''
          # table_prefix: ''
          # s3_target: 
          #   -
          #     path: ''
          #     exclusions: 
          #       -
          # jdbc_target: 
          #   -
          #     path: ''
          #     connection_name: ''
          #     exclusions: 
          #       -
          # configuration: ''
          # description: ''
          # schedule: ''
          # classifiers: []
    variable:
      {{ name }}_name:
        type: string
      {{ name }}_role:
        type: string
      {{ name }}_database_name:
        type: string
    output:
      id:
        value: aws_glue_crawler.{{ name }}.id
      thub_id:
        value: aws_glue_crawler.{{ name }}.id
      arn:
        value: aws_glue_crawler.{{ name }}.arn
