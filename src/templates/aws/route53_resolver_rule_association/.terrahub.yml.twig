  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_route53_resolver_rule_association:
        {{ name }}:
          resolver_rule_id: var.{{ name }}_resolver_rule_id
          vpc_id: var.{{ name }}_vpc_id
          # name: ''
    variable:
      {{ name }}_resolver_rule_id:
        type: string
      {{ name }}_vpc_id:
        type: string
    output:
      id:
        value: aws_route53_resolver_rule_association.{{ name }}.id
      thub_id:
        value: aws_route53_resolver_rule_association.{{ name }}.id
