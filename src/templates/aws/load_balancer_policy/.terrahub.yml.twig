  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_load_balancer_policy:
        {{ name }}:
          load_balancer_name: '${var.{{ name }}_load_balancer_name}'
          policy_name: '${var.{{ name }}_policy_name}'
          policy_type_name: '${var.{{ name }}_policy_type_name}'
          # policy_attribute:
          #   name: ''
          #   value: ''
    variable:
      {{ name }}_load_balancer_name:
        type: 'string'
      {{ name }}_policy_name:
        type: 'string'
      {{ name }}_policy_type_name:
        type: 'string'
    output:
      id:
        value: '${aws_load_balancer_policy.{{ name }}.id}'
      thub_id:
        value: '${aws_load_balancer_policy.{{ name }}.id}'
