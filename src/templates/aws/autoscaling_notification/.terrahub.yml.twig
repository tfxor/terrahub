  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_autoscaling_notification:
        {{ name }}:
          topic_arn: '${var.{{ name }}_topic_arn}'
          group_names: '${var.{{ name }}_group_names}'
          notifications: '${var.{{ name }}_notifications}'
    variable:
      {{ name }}_topic_arn:
        type: 'string'
      {{ name }}_group_names:
        type: 'set'
      {{ name }}_notifications:
        type: 'set'
    output:
      id:
        value: '${aws_autoscaling_notification.{{ name }}.id}'
      thub_id:
        value: '${aws_autoscaling_notification.{{ name }}.id}'
