  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_ssm_document:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          document_type: '${var.{{ name }}_document_type}'
          content: '${var.{{ name }}_content}'
          # tags: 
          #   '[TO_BE_REPLACED]': ''
          # permissions: 
          #   '[TO_BE_REPLACED]': ''
          #   'type': ''
          #   'account_ids': ''
          # document_format: ''
    variable:
      {{ name }}_name:
        type: 'string'
      {{ name }}_document_type:
        type: 'string'
      {{ name }}_content:
        type: 'string'
    output:
      id:
        value: '${aws_ssm_document.{{ name }}.id}'
      thub_id:
        value: '${aws_ssm_document.{{ name }}.id}'
      status:
        value: '${aws_ssm_document.{{ name }}.status}'
      schema_version:
        value: '${aws_ssm_document.{{ name }}.schema_version}'
      created_date:
        value: '${aws_ssm_document.{{ name }}.created_date}'
      latest_version:
        value: '${aws_ssm_document.{{ name }}.latest_version}'
      platform_types:
        value: '${aws_ssm_document.{{ name }}.platform_types}'
      description:
        value: '${aws_ssm_document.{{ name }}.description}'
      hash:
        value: '${aws_ssm_document.{{ name }}.hash}'
      hash_type:
        value: '${aws_ssm_document.{{ name }}.hash_type}'
      parameter:
        value: '${aws_ssm_document.{{ name }}.parameter}'
      arn:
        value: '${aws_ssm_document.{{ name }}.arn}'
      default_version:
        value: '${aws_ssm_document.{{ name }}.default_version}'
      owner:
        value: '${aws_ssm_document.{{ name }}.owner}'
