  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_storagegateway_smb_file_share:
        {{ name }}:
          gateway_arn: '${var.{{ name }}_gateway_arn}'
          role_arn: '${var.{{ name }}_role_arn}'
          location_arn: '${var.{{ name }}_location_arn}'
          # requester_pays: false
          # kms_encrypted: false
          # kms_key_arn: ''
          # object_acl: ''
          # authentication: ''
          # guess_mime_type_enabled: false
          # valid_user_list: []
          # default_storage_class: ''
          # invalid_user_list: []
          # read_only: false
    variable:
      {{ name }}_gateway_arn:
        type: 'string'
      {{ name }}_role_arn:
        type: 'string'
      {{ name }}_location_arn:
        type: 'string'
    output:
      id:
        value: '${aws_storagegateway_smb_file_share.{{ name }}.id}'
      thub_id:
        value: '${aws_storagegateway_smb_file_share.{{ name }}.id}'
      fileshare_id:
        value: '${aws_storagegateway_smb_file_share.{{ name }}.fileshare_id}'
      arn:
        value: '${aws_storagegateway_smb_file_share.{{ name }}.arn}'
