  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_db_parameter_group:
        {{ name }}:
          family: '${var.{{ name }}_family}'
          # name: ''
          # name_prefix: ''
          # description: ''
          # parameter: 
          #   name:''
          #   value:''
          #   apply_method:''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_family:
        type: 'string'
    output:
      id:
        value: '${aws_db_parameter_group.{{ name }}.id}'
      thub_id:
        value: '${aws_db_parameter_group.{{ name }}.id}'
      name:
        value: '${aws_db_parameter_group.{{ name }}.name}'
      name_prefix:
        value: '${aws_db_parameter_group.{{ name }}.name_prefix}'
      arn:
        value: '${aws_db_parameter_group.{{ name }}.arn}'
