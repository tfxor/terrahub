  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_ec2_transit_gateway_vpc_attachment:
        {{ name }}:
          subnet_ids: '${var.{{ name }}_subnet_ids}'
          transit_gateway_id: '${var.{{ name }}_transit_gateway_id}'
          vpc_id: '${var.{{ name }}_vpc_id}'
          # dns_support: ''
          # transit_gateway_default_route_table_propagation: false
          # ipv6_support: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
          # transit_gateway_default_route_table_association: false
    variable:
      {{ name }}_subnet_ids:
        type: 'set'
      {{ name }}_transit_gateway_id:
        type: 'string'
      {{ name }}_vpc_id:
        type: 'string'
    output:
      id:
        value: '${aws_ec2_transit_gateway_vpc_attachment.{{ name }}.id}'
      thub_id:
        value: '${aws_ec2_transit_gateway_vpc_attachment.{{ name }}.id}'
      vpc_owner_id:
        value: '${aws_ec2_transit_gateway_vpc_attachment.{{ name }}.vpc_owner_id}'
