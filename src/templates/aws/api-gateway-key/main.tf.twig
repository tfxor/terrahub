resource "aws_api_gateway_api_key" "{{ name }}" {
  provider    = "aws.default"
  name        = "${var.api_gateway_key_name}"
  description = "${var.api_gateway_key_description}"
  enabled     = "${var.api_gateway_key_enabled}"
}
