  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_ebs_encryption_by_default:
        {{ name }}:
          # enabled: false
    output:
      id:
        value: aws_ebs_encryption_by_default.{{ name }}.id
      thub_id:
        value: aws_ebs_encryption_by_default.{{ name }}.id
