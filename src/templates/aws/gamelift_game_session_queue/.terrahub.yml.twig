  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_gamelift_game_session_queue:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          # destinations: []
          # player_latency_policy: 
          #   -
          #     maximum_individual_player_latency_milliseconds: 0
          #     policy_duration_seconds: 0
          # timeout_in_seconds: 0
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_gamelift_game_session_queue.{{ name }}.id}'
      thub_id:
        value: '${aws_gamelift_game_session_queue.{{ name }}.id}'
      arn:
        value: '${aws_gamelift_game_session_queue.{{ name }}.arn}'
