  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_acm_certificate_validation:
        {{ name }}:
          certificate_arn: '${var.{{ name }}_certificate_arn}'
          # validation_record_fqdns: []
    variable:
      {{ name }}_certificate_arn:
        type: 'string'
    output:
      id:
        value: '${aws_acm_certificate_validation.{{ name }}.id}'
      thub_id:
        value: '${aws_acm_certificate_validation.{{ name }}.id}'
