  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_api_gateway_vpc_link:
        {{ name }}:
          name: var.{{ name }}_name
          target_arns: var.{{ name }}_target_arns
          # description: ''
    variable:
      {{ name }}_name:
        type: string
      {{ name }}_target_arns:
        type: set
    output:
      id:
        value: aws_api_gateway_vpc_link.{{ name }}.id
      thub_id:
        value: aws_api_gateway_vpc_link.{{ name }}.id
