  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_ssm_maintenance_window_target:
        {{ name }}:
          targets:
            -
              key: ''
              values: []
          window_id: '${var.{{ name }}_window_id}'
          resource_type: '${var.{{ name }}_resource_type}'
          # owner_information: ''
    variable:
      {{ name }}_window_id:
        type: 'string'
      {{ name }}_resource_type:
        type: 'string'
    output:
      id:
        value: '${aws_ssm_maintenance_window_target.{{ name }}.id}'
      thub_id:
        value: '${aws_ssm_maintenance_window_target.{{ name }}.id}'
