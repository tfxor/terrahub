  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_redshift_subnet_group:
        {{ name }}:
          subnet_ids: '${var.{{ name }}_subnet_ids}'
          name: '${var.{{ name }}_name}'
          # tags: 
          #   '[TO_BE_REPLACED]': ''
          # description: ''
    variable:
      {{ name }}_subnet_ids:
        type: 'set'
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_redshift_subnet_group.{{ name }}.id}'
      thub_id:
        value: '${aws_redshift_subnet_group.{{ name }}.id}'
