  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_pinpoint_email_channel:
        {{ name }}:
          identity: '${var.{{ name }}_identity}'
          role_arn: '${var.{{ name }}_role_arn}'
          application_id: '${var.{{ name }}_application_id}'
          from_address: '${var.{{ name }}_from_address}'
          # enabled: false
    variable:
      {{ name }}_identity:
        type: 'string'
      {{ name }}_role_arn:
        type: 'string'
      {{ name }}_application_id:
        type: 'string'
      {{ name }}_from_address:
        type: 'string'
    output:
      id:
        value: '${aws_pinpoint_email_channel.{{ name }}.id}'
      thub_id:
        value: '${aws_pinpoint_email_channel.{{ name }}.id}'
      messages_per_second:
        value: '${aws_pinpoint_email_channel.{{ name }}.messages_per_second}'
