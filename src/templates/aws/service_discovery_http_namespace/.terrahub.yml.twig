  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_service_discovery_http_namespace:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          # description: ''
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_service_discovery_http_namespace.{{ name }}.id}'
      thub_id:
        value: '${aws_service_discovery_http_namespace.{{ name }}.id}'
      arn:
        value: '${aws_service_discovery_http_namespace.{{ name }}.arn}'
