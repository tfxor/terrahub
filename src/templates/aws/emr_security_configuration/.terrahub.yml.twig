  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_emr_security_configuration:
        {{ name }}:
          configuration: '${var.{{ name }}_configuration}'
          # name_prefix: ''
          # name: ''
    variable:
      {{ name }}_configuration:
        type: 'string'
    output:
      id:
        value: '${aws_emr_security_configuration.{{ name }}.id}'
      thub_id:
        value: '${aws_emr_security_configuration.{{ name }}.id}'
      creation_date:
        value: '${aws_emr_security_configuration.{{ name }}.creation_date}'
      name:
        value: '${aws_emr_security_configuration.{{ name }}.name}'
