  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_datasync_location_efs:
        {{ name }}:
          ec2_config: 
            -
              security_group_arns: []
              subnet_arn: ''
          efs_file_system_arn: '${var.{{ name }}_efs_file_system_arn}'
          # subdirectory: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_efs_file_system_arn:
        type: 'string'
    output:
      id:
        value: '${aws_datasync_location_efs.{{ name }}.id}'
      thub_id:
        value: '${aws_datasync_location_efs.{{ name }}.id}'
      arn:
        value: '${aws_datasync_location_efs.{{ name }}.arn}'
      uri:
        value: '${aws_datasync_location_efs.{{ name }}.uri}'
