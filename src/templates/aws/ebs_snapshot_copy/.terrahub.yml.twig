  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_ebs_snapshot_copy:
        {{ name }}:
          source_region: '${var.{{ name }}_source_region}'
          source_snapshot_id: '${var.{{ name }}_source_snapshot_id}'
          # description: ''
          # encrypted: false
          # kms_key_id: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_source_region:
        type: 'string'
      {{ name }}_source_snapshot_id:
        type: 'string'
    output:
      id:
        value: '${aws_ebs_snapshot_copy.{{ name }}.id}'
      thub_id:
        value: '${aws_ebs_snapshot_copy.{{ name }}.id}'
      owner_id:
        value: '${aws_ebs_snapshot_copy.{{ name }}.owner_id}'
      owner_alias:
        value: '${aws_ebs_snapshot_copy.{{ name }}.owner_alias}'
      volume_id:
        value: '${aws_ebs_snapshot_copy.{{ name }}.volume_id}'
      data_encryption_key_id:
        value: '${aws_ebs_snapshot_copy.{{ name }}.data_encryption_key_id}'
      volume_size:
        value: '${aws_ebs_snapshot_copy.{{ name }}.volume_size}'
