  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_elastic_beanstalk_configuration_template:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          application: '${var.{{ name }}_application}'
          # setting: 
          # solution_stack_name: ''
          # description: ''
          # environment_id: ''
    variable:
      {{ name }}_name:
        type: 'string'
      {{ name }}_application:
        type: 'string'
    output:
      id:
        value: '${aws_elastic_beanstalk_configuration_template.{{ name }}.id}'
      thub_id:
        value: '${aws_elastic_beanstalk_configuration_template.{{ name }}.id}'
      setting:
        value: '${aws_elastic_beanstalk_configuration_template.{{ name }}.setting}'
