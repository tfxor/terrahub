  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_sqs_queue:
        {{ name }}:
          # visibility_timeout_seconds: 0
          # redrive_policy: ''
          # delay_seconds: 0
          # message_retention_seconds: 0
          # policy: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
          # name: ''
          # name_prefix: ''
          # receive_wait_time_seconds: 0
          # kms_master_key_id: ''
          # kms_data_key_reuse_period_seconds: 0
          # max_message_size: 0
    output:
      id:
        value: aws_sqs_queue.{{ name }}.id
      thub_id:
        value: aws_sqs_queue.{{ name }}.id
      policy:
        value: aws_sqs_queue.{{ name }}.policy
      name:
        value: aws_sqs_queue.{{ name }}.name
      kms_data_key_reuse_period_seconds:
        value: aws_sqs_queue.{{ name }}.kms_data_key_reuse_period_seconds
      arn:
        value: aws_sqs_queue.{{ name }}.arn
