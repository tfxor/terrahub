  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_wafregional_regex_pattern_set:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          # regex_pattern_strings: []
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_wafregional_regex_pattern_set.{{ name }}.id}'
      thub_id:
        value: '${aws_wafregional_regex_pattern_set.{{ name }}.id}'
