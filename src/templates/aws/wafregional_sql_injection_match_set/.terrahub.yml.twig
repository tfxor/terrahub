  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_wafregional_sql_injection_match_set:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          # sql_injection_match_tuple: 
          #   field_to_match:
          #     -
          #       type: ''
          #       data: ''
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_wafregional_sql_injection_match_set.{{ name }}.id}'
      thub_id:
        value: '${aws_wafregional_sql_injection_match_set.{{ name }}.id}'
