  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_waf_geo_match_set:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          # geo_match_constraint: 
          #   type: ''
          #   value: ''
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_waf_geo_match_set.{{ name }}.id}'
      thub_id:
        value: '${aws_waf_geo_match_set.{{ name }}.id}'
