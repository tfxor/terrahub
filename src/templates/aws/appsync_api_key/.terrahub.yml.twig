  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_appsync_api_key:
        {{ name }}:
          api_id: '${var.{{ name }}_api_id}'
          # description: ''
          # expires: ''
    variable:
      {{ name }}_api_id:
        type: 'string'
    output:
      id:
        value: '${aws_appsync_api_key.{{ name }}.id}'
      thub_id:
        value: '${aws_appsync_api_key.{{ name }}.id}'
      key:
        value: '${aws_appsync_api_key.{{ name }}.key}'
