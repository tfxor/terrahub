  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_cognito_user_group:
        {{ name }}:
          user_pool_id: var.{{ name }}_user_pool_id
          name: var.{{ name }}_name
          # precedence: 0
          # role_arn: ''
          # description: ''
    variable:
      {{ name }}_user_pool_id:
        type: string
      {{ name }}_name:
        type: string
    output:
      id:
        value: aws_cognito_user_group.{{ name }}.id
      thub_id:
        value: aws_cognito_user_group.{{ name }}.id
