resource "aws_lb_ssl_negotiation_policy" "{{ name }}" {
  name          = "${var.lb_ssl_negotiation_policy_name}"
  load_balancer = "${var.lb_ssl_negotiation_policy_lb}"
  lb_port       = "${var.lb_ssl_negotiation_policy_lb_port"

  attribut = "${var.lb_ssl_negotiation_policy_attribut}"
}