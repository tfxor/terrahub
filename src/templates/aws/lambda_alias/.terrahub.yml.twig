  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_lambda_alias:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          function_name: '${var.{{ name }}_function_name}'
          function_version: '${var.{{ name }}_function_version}'
          # routing_config: 
          #   -
          #     additional_version_weights: 
          #       '[TO_BE_REPLACED]': ''
          # description: ''
    variable:
      {{ name }}_name:
        type: 'string'
      {{ name }}_function_name:
        type: 'string'
      {{ name }}_function_version:
        type: 'string'
    output:
      id:
        value: '${aws_lambda_alias.{{ name }}.id}'
      thub_id:
        value: '${aws_lambda_alias.{{ name }}.id}'
      arn:
        value: '${aws_lambda_alias.{{ name }}.arn}'
      invoke_arn:
        value: '${aws_lambda_alias.{{ name }}.invoke_arn}'
