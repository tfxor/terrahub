  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_backup_plan:
        {{ name }}:
          name: var.{{ name }}_name
          rule: 
            rule_name: ''
            target_vault_name: ''
          #   completion_window: 0
          #   lifecycle: 
          #     -
          #       cold_storage_after: 0
          #       delete_after: 0
          #   schedule: ''
          #   start_window: 0
          # tags: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_name:
        type: string
    output:
      id:
        value: aws_backup_plan.{{ name }}.id
      thub_id:
        value: aws_backup_plan.{{ name }}.id
      arn:
        value: aws_backup_plan.{{ name }}.arn
      version:
        value: aws_backup_plan.{{ name }}.version
