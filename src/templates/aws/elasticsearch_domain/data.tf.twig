data "aws_iam_policy_document" "{{ name }}" {
  statement {
    actions   = "es:*"
    resources = ["arn:aws:es:${var.region}:${var.account_id}:domain/${var.aws_es_domain_name}/*"]

    principals = {
      type        = "AWS"
      identifiers = "*"
    }

    effect = "Allow"
  }
}
