  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_quicksight_group:
        {{ name }}:
          group_name: var.{{ name }}_group_name
          # aws_account_id: ''
          # description: ''
          # namespace: ''
    variable:
      {{ name }}_group_name:
        type: string
    output:
      id:
        value: aws_quicksight_group.{{ name }}.id
      thub_id:
        value: aws_quicksight_group.{{ name }}.id
      arn:
        value: aws_quicksight_group.{{ name }}.arn
      aws_account_id:
        value: aws_quicksight_group.{{ name }}.aws_account_id
