  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_ses_receipt_filter:
        {{ name }}:
          policy: '${var.{{ name }}_policy}'
          name: '${var.{{ name }}_name}'
          cidr: '${var.{{ name }}_cidr}'
    variable:
      {{ name }}_policy:
        type: 'string'
      {{ name }}_name:
        type: 'string'
      {{ name }}_cidr:
        type: 'string'
    output:
      id:
        value: '${aws_ses_receipt_filter.{{ name }}.id}'
      thub_id:
        value: '${aws_ses_receipt_filter.{{ name }}.id}'
