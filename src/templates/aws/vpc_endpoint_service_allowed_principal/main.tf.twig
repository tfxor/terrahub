resource "aws_vpc_endpoint_service_allowed_principal" "{{ name }}" {
  vpc_endpoint_service_id = "${var.vpc_endpoint_vpc_endpoint_service_id}"
  principal_arn           = "${var.vpc_endpoint_principal_arn}"
}