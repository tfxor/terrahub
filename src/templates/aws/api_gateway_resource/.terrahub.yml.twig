  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_api_gateway_resource:
        {{ name }}:
          rest_api_id: var.{{ name }}_rest_api_id
          parent_id: var.{{ name }}_parent_id
          path_part: var.{{ name }}_path_part
    variable:
      {{ name }}_rest_api_id:
        type: string
      {{ name }}_parent_id:
        type: string
      {{ name }}_path_part:
        type: string
    output:
      id:
        value: aws_api_gateway_resource.{{ name }}.id
      thub_id:
        value: aws_api_gateway_resource.{{ name }}.id
      path:
        value: aws_api_gateway_resource.{{ name }}.path
