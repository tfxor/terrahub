  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_dax_parameter_group:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          # description: ''
          # parameters: 
          #   name: ''
          #   value: ''
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_dax_parameter_group.{{ name }}.id}'
      thub_id:
        value: '${aws_dax_parameter_group.{{ name }}.id}'
      parameters:
        value: '${aws_dax_parameter_group.{{ name }}.parameters}'
