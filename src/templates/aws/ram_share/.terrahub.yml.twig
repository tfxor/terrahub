  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_ram_share:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          # tags:
          #   '[TO_BE_REPLACED]': ''
          # allow_external_principals: false
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_ram_share.{{ name }}.id}'
      thub_id:
        value: '${aws_ram_share.{{ name }}.id}'
