  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_lightsail_key_pair:
        {{ name }}:
          # name: ''
          # pgp_key: ''
          # public_key: ''
          # name_prefix: ''
    output:
      id:
        value: '${aws_lightsail_key_pair.{{ name }}.id}'
      thub_id:
        value: '${aws_lightsail_key_pair.{{ name }}.id}'
      private_key:
        value: '${aws_lightsail_key_pair.{{ name }}.private_key}'
      encrypted_private_key:
        value: '${aws_lightsail_key_pair.{{ name }}.encrypted_private_key}'
      name:
        value: '${aws_lightsail_key_pair.{{ name }}.name}'
      arn:
        value: '${aws_lightsail_key_pair.{{ name }}.arn}'
      fingerprint:
        value: '${aws_lightsail_key_pair.{{ name }}.fingerprint}'
      public_key:
        value: '${aws_lightsail_key_pair.{{ name }}.public_key}'
      encrypted_fingerprint:
        value: '${aws_lightsail_key_pair.{{ name }}.encrypted_fingerprint}'
