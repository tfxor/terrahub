  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_api_gateway_model:
        {{ name }}:
          rest_api_id: '${var.{{ name }}_rest_api_id}'
          name: '${var.{{ name }}_name}'
          content_type: '${var.{{ name }}_content_type}'
          # description: ''
          # schema: ''
    variable:
      {{ name }}_rest_api_id:
        type: 'string'
      {{ name }}_name:
        type: 'string'
      {{ name }}_content_type:
        type: 'string'
    output:
      id:
        value: '${aws_api_gateway_model.{{ name }}.id}'
      thub_id:
        value: '${aws_api_gateway_model.{{ name }}.id}'
