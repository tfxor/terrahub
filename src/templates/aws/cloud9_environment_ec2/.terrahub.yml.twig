  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_cloud9_environment_ec2:
        {{ name }}:
          instance_type: '${var.{{ name }}_instance_type}'
          name: '${var.{{ name }}_name}'
          # automatic_stop_time_minutes: 0
          # description: ''
          # owner_arn: ''
          # subnet_id: ''
    variable:
      {{ name }}_instance_type:
        type: 'string'
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_cloud9_environment_ec2.{{ name }}.id}'
      thub_id:
        value: '${aws_cloud9_environment_ec2.{{ name }}.id}'
      owner_arn:
        value: '${aws_cloud9_environment_ec2.{{ name }}.owner_arn}'
      arn:
        value: '${aws_cloud9_environment_ec2.{{ name }}.arn}'
      type:
        value: '${aws_cloud9_environment_ec2.{{ name }}.type}'
