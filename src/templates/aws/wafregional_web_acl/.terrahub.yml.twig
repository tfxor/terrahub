  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_wafregional_web_acl:
        {{ name }}:
          priority: var.{{ name }}_priority
          rule_id: var.{{ name }}_rule_id
          name: var.{{ name }}_name
          metric_name: var.{{ name }}_metric_name
          default_action: 
            -
              type: ''
          # type: ''
          # rule: 
          #   action: 
          #     -
          #       type: ''
          # logging_configuration: 
          #   -
          #     log_destination: ''
          #     redacted_fields: 
          #       -
          #         field_to_match: 
          #       type: ''
          #       data: ''
          # override_action: 
          #   -
          #     type: ''
    variable:
      {{ name }}_priority:
        type: int
      {{ name }}_rule_id:
        type: string
      {{ name }}_name:
        type: string
      {{ name }}_metric_name:
        type: string
    output:
      id:
        value: aws_wafregional_web_acl.{{ name }}.id
      thub_id:
        value: aws_wafregional_web_acl.{{ name }}.id
      arn:
        value: aws_wafregional_web_acl.{{ name }}.arn
