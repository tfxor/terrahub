  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_glue_connection:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          connection_properties: '${var.{{ name }}_connection_properties}'
          # match_criteria: []
          # physical_connection_requirements: 
          #   -
          #     security_group_id_list: 
          #       -
          #     subnet_id: ''
          #     availability_zone: ''
          # catalog_id: ''
          # connection_type: ''
          # description: ''
    variable:
      {{ name }}_name:
        type: 'string'
      {{ name }}_connection_properties:
        type: 'map'
    output:
      id:
        value: '${aws_glue_connection.{{ name }}.id}'
      thub_id:
        value: '${aws_glue_connection.{{ name }}.id}'
      catalog_id:
        value: '${aws_glue_connection.{{ name }}.catalog_id}'
