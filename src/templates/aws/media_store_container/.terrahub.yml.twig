  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_media_store_container:
        {{ name }}:
          name: var.{{ name }}_name
          # tags: 
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_name:
        type: string
    output:
      id:
        value: aws_media_store_container.{{ name }}.id
      thub_id:
        value: aws_media_store_container.{{ name }}.id
      arn:
        value: aws_media_store_container.{{ name }}.arn
      endpoint:
        value: aws_media_store_container.{{ name }}.endpoint
