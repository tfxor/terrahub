  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_budgets_budget:
        {{ name }}:
          budget_type: '${var.{{ name }}_budget_type}'
          limit_amount: '${var.{{ name }}_limit_amount}'
          limit_unit: '${var.{{ name }}_limit_unit}'
          time_period_start: '${var.{{ name }}_time_period_start}'
          time_unit: '${var.{{ name }}_time_unit}'
          # cost_filters: 
          #   '[TO_BE_REPLACED]': ''
          # account_id: ''
          # name_prefix: ''
          # cost_types: 
          #   -
          #     include_tax: false
          #     include_upfront: false
          #     use_amortized: false
          #     include_credit: false
          #     include_discount: false
          #     include_other_subscription: false
          #     include_refund: false
          #     include_support: false
          #     include_recurring: false
          #     include_subscription: false
          #     use_blended: false
          # time_period_end: ''
          # name: ''
    variable:
      {{ name }}_budget_type:
        type: 'string'
      {{ name }}_limit_amount:
        type: 'string'
      {{ name }}_limit_unit:
        type: 'string'
      {{ name }}_time_period_start:
        type: 'string'
      {{ name }}_time_unit:
        type: 'string'
    output:
      id:
        value: '${aws_budgets_budget.{{ name }}.id}'
      thub_id:
        value: '${aws_budgets_budget.{{ name }}.id}'
      cost_filters:
        value: '${aws_budgets_budget.{{ name }}.cost_filters}'
      account_id:
        value: '${aws_budgets_budget.{{ name }}.account_id}'
      name_prefix:
        value: '${aws_budgets_budget.{{ name }}.name_prefix}'
      cost_types:
        value: '${aws_budgets_budget.{{ name }}.cost_types}'
      name:
        value: '${aws_budgets_budget.{{ name }}.name}'
