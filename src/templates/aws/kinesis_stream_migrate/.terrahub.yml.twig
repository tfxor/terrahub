  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_kinesis_stream_migrate:
        {{ name }}:
          name: var.{{ name }}_name
          shard_count: var.{{ name }}_shard_count
          # tags: 
          #   '[TO_BE_REPLACED]': ''
          # retention_period: 0
          # shard_level_metrics: []
          # encryption_type: ''
          # kms_key_id: ''
          # arn: ''
    variable:
      {{ name }}_name:
        type: string
      {{ name }}_shard_count:
        type: int
    output:
      arn:
        value: aws_kinesis_stream_migrate.{{ name }}.arn
