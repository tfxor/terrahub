  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_iam_policy_attachment:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          policy_arn: '${var.{{ name }}_policy_arn}'
          # users: []
          # roles: []
          # groups: []
    variable:
      {{ name }}_name:
        type: 'string'
      {{ name }}_policy_arn:
        type: 'string'
    output:
      id:
        value: '${aws_iam_policy_attachment.{{ name }}.id}'
      thub_id:
        value: '${aws_iam_policy_attachment.{{ name }}.id}'
