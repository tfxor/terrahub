  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_media_package_channel:
        {{ name }}:
          channel_id: '${var.{{ name }}_channel_id}'
          # description: ''
    variable:
      {{ name }}_channel_id:
        type: 'string'
    output:
      id:
        value: '${aws_media_package_channel.{{ name }}.id}'
      thub_id:
        value: '${aws_media_package_channel.{{ name }}.id}'
      arn:
        value: '${aws_media_package_channel.{{ name }}.arn}'
      hls_ingest:
        value: '${aws_media_package_channel.{{ name }}.hls_ingest}'
