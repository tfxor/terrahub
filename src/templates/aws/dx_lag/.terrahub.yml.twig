  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_dx_lag:
        {{ name }}:
          location: '${var.{{ name }}_location}'
          name: '${var.{{ name }}_name}'
          connections_bandwidth: '${var.{{ name }}_connections_bandwidth}'
          # number_of_connections: 0
          # force_destroy: false
          # tags:
          #   '[TO_BE_REPLACED]': ''
    variable:
      {{ name }}_location:
        type: 'string'
      {{ name }}_name:
        type: 'string'
      {{ name }}_connections_bandwidth:
        type: 'string'
    output:
      id:
        value: '${aws_dx_lag.{{ name }}.id}'
      thub_id:
        value: '${aws_dx_lag.{{ name }}.id}'
      number_of_connections:
        value: '${aws_dx_lag.{{ name }}.number_of_connections}'
      arn:
        value: '${aws_dx_lag.{{ name }}.arn}'
