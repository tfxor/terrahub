  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_waf_rule:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          metric_name: '${var.{{ name }}_metric_name}'
          # predicates: 
          #   type: ''
          #   negated: false
          #   data_id: ''
    variable:
      {{ name }}_name:
        type: 'string'
      {{ name }}_metric_name:
        type: 'string'
    output:
      id:
        value: '${aws_waf_rule.{{ name }}.id}'
      thub_id:
        value: '${aws_waf_rule.{{ name }}.id}'
