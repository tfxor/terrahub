# Define list of variables to be output

output "device_name" {
  value = "${aws_volume_attachment.{{ name }}.device_name}"
}

output "instance_id" {
  value = "${aws_volume_attachment.{{ name }}.instance_id}"
}

output "volume_id" {
  value = "${aws_volume_attachment.{{ name }}.volume_id}"
}
