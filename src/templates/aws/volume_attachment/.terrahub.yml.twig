  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_volume_attachment:
        {{ name }}:
          device_name: var.{{ name }}_device_name
          instance_id: var.{{ name }}_instance_id
          volume_id: var.{{ name }}_volume_id
          # force_detach: false
          # skip_destroy: false
    variable:
      {{ name }}_device_name:
        type: string
      {{ name }}_instance_id:
        type: string
      {{ name }}_volume_id:
        type: string
    output:
      id:
        value: aws_volume_attachment.{{ name }}.id
      thub_id:
        value: aws_volume_attachment.{{ name }}.id
