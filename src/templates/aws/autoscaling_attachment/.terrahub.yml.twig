  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_autoscaling_attachment:
        {{ name }}:
          autoscaling_group_name: var.{{ name }}_autoscaling_group_name
          # alb_target_group_arn: ''
          # elb: ''
    variable:
      {{ name }}_autoscaling_group_name:
        type: string
    output:
      id:
        value: aws_autoscaling_attachment.{{ name }}.id
      thub_id:
        value: aws_autoscaling_attachment.{{ name }}.id
