  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_lightsail_domain:
        {{ name }}:
          domain_name: '${var.{{ name }}_domain_name}'
    variable:
      {{ name }}_domain_name:
        type: 'string'
    output:
      id:
        value: '${aws_lightsail_domain.{{ name }}.id}'
      thub_id:
        value: '${aws_lightsail_domain.{{ name }}.id}'
      arn:
        value: '${aws_lightsail_domain.{{ name }}.arn}'
