  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_iam_group_policy:
        {{ name }}:
          policy: '${var.{{ name }}_policy}'
          group: '${var.{{ name }}_group}'
          # name: ''
          # name_prefix: ''
    variable:
      {{ name }}_policy:
        type: 'string'
      {{ name }}_group:
        type: 'string'
    output:
      id:
        value: '${aws_iam_group_policy.{{ name }}.id}'
      thub_id:
        value: '${aws_iam_group_policy.{{ name }}.id}'
      name:
        value: '${aws_iam_group_policy.{{ name }}.name}'
