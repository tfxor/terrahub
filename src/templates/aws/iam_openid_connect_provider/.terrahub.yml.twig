  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_iam_openid_connect_provider:
        {{ name }}:
          client_id_list: var.{{ name }}_client_id_list
          thumbprint_list: var.{{ name }}_thumbprint_list
          url: var.{{ name }}_url
    variable:
      {{ name }}_client_id_list:
        type: list
      {{ name }}_thumbprint_list:
        type: list
      {{ name }}_url:
        type: string
    output:
      id:
        value: aws_iam_openid_connect_provider.{{ name }}.id
      thub_id:
        value: aws_iam_openid_connect_provider.{{ name }}.id
      arn:
        value: aws_iam_openid_connect_provider.{{ name }}.arn
