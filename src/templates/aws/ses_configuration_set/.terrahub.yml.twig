  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_ses_configuration_set:
        {{ name }}:
          name: '${var.{{ name }}_name}'
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_ses_configuration_set.{{ name }}.id}'
      thub_id:
        value: '${aws_ses_configuration_set.{{ name }}.id}'
