  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_codecommit_repository:
        {{ name }}:
          repository_name: var.{{ name }}_repository_name
          # default_branch: ''
          # tags: 
          #   '[TO_BE_REPLACED]': ''
          # description: ''
    variable:
      {{ name }}_repository_name:
        type: string
    output:
      id:
        value: aws_codecommit_repository.{{ name }}.id
      thub_id:
        value: aws_codecommit_repository.{{ name }}.id
      arn:
        value: aws_codecommit_repository.{{ name }}.arn
      repository_id:
        value: aws_codecommit_repository.{{ name }}.repository_id
      clone_url_http:
        value: aws_codecommit_repository.{{ name }}.clone_url_http
      clone_url_ssh:
        value: aws_codecommit_repository.{{ name }}.clone_url_ssh
