  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_placement_group:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          strategy: '${var.{{ name }}_strategy}'
    variable:
      {{ name }}_name:
        type: 'string'
      {{ name }}_strategy:
        type: 'string'
    output:
      id:
        value: '${aws_placement_group.{{ name }}.id}'
      thub_id:
        value: '${aws_placement_group.{{ name }}.id}'
