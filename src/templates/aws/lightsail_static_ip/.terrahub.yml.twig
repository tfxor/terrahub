  template:
    # terraform:
    #   backend:
    #     s3:
    #     key: 'path/to/terraform.tfstate'
    resource:
      aws_lightsail_static_ip:
        {{ name }}:
          name: '${var.{{ name }}_name}'
    variable:
      {{ name }}_name:
        type: 'string'
    output:
      id:
        value: '${aws_lightsail_static_ip.{{ name }}.id}'
      thub_id:
        value: '${aws_lightsail_static_ip.{{ name }}.id}'
      ip_address:
        value: '${aws_lightsail_static_ip.{{ name }}.ip_address}'
      arn:
        value: '${aws_lightsail_static_ip.{{ name }}.arn}'
      support_code:
        value: '${aws_lightsail_static_ip.{{ name }}.support_code}'
