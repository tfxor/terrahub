  template:
    # terraform:
    #   backend:
    #     s3:
    #       region: 'us-east-1'
    #       bucket: 'bucket-name'
    #       key: 'path/to/terraform.tfstate'
    resource:
      aws_elasticache_security_group:
        {{ name }}:
          name: '${var.{{ name }}_name}'
          security_group_names: '${var.{{ name }}_security_group_names}'
          # description: ''
    variable:
      {{ name }}_name:
        type: 'string'
      {{ name }}_security_group_names:
        type: 'set'
    output:
      id:
        value: '${aws_elasticache_security_group.{{ name }}.id}'
      thub_id:
        value: '${aws_elasticache_security_group.{{ name }}.id}'
